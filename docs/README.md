# 餐厅 POS 旁路会员积分系统 — 文档索引

> 本目录为**网络不可见**目录，存放全部系统设计文档、接口说明与数据库字典。

## 文档清单

| 文档 | 内容 |
|---|---|
| [00-架构总览.md](./00-架构总览.md) | 旁路监听架构、网络策略、部署拓扑、目录规范 |
| [01-POS主库数据字典.md](./01-POS主库数据字典.md) | 主库字段语义、实测结论、坑位清单 |
| [02-只读接入规范.md](./02-只读接入规范.md) | 性能铁律、SQL 模板、水位线、超时兜底 |
| [03-积分与防刷引擎.md](./03-积分与防刷引擎.md) | 订单定位、AA 分摊、幂等、退单判据、校准任务 |
| [04-本地库Schema.md](./04-本地库Schema.md) | 建表 DDL、多店维度、账本设计 |
| [05-合规与Wallet.md](./05-合规与Wallet.md) | LOPDGDD 落地、双重确认、Wallet 卡分发 |
| [06-部署手册.md](./06-部署手册.md) | 环境要求、安装步骤、Nginx 配置、运维与故障速查 |

## 代码

| 目录 | 内容 |
|---|---|
| `wwwroot/` | **唯一网络可见目录**：Pad 前端与 API 入口、CP 后台 |
| `app/` | 业务逻辑（网络不可见）：`lib/` 核心、`api/` Pad 路由、`cp/` 后台路由 |
| `bin/` | CLI 入口：`init.php` 初始化、`cron.php` 定时任务 |
| `db/` | DDL、种子、MySQL/MariaDB 双兼容约定 |
| `tests/` | `run.php` 纯逻辑（208 项）、`smoke.php` 真本地库冒烟（100 项）、`e2e_pos.php` 真 POS+真本地库端到端（75 项）、`sim/` 模拟环境搭建 |

## 核心前提（不可动摇）

1. **POS 主库完全只读。** 不写入、不改表、不改配置、不装插件。所有交互仅 `SELECT`。
2. **POS 主机性能极度受限。** 任何查询都必须命中索引、限制步长、限定字段。
3. **网络单向。** 门店可访问外网，外网不可访问门店。

## 数据分析基线

本文档中所有"实测"结论，来源于以下三份主库导出：

| 文件 | 来源主机 | 内容 | 规模 |
|---|---|---|---|
| `pdb/192_168_2_40 (1).sql` | `192.168.2.40`（主库） | 主库完整结构（无数据） | 190 张表 |
| `history_order_head.sql`（上传件） | `192.168.58.128`（旧侧系统） | 历史订单头**全量数据** | **88,616 行** |
| `history_order_detail.sql` | `192.168.58.128`（旧侧系统） | 历史订单明细样本 | 100 行 |
| `order_detail.sql` | `192.168.58.128`（旧侧系统） | 菜单 + 活动订单明细样本 | 667 个菜品 |
| `coolroid.sql`（上传件） | `192.168.1.180`（现侧系统） | `major_group` / `family_group` / `history_major_group` | 4 + 9 + 1364 行 |
| `history_order_detail.sql`（上传件 2） | `192.168.1.180`（现侧系统） | 历史订单明细（2024-01-25~27），160 个订单 | **5,000 行** |
| `history_order_detail2.sql`（上传件 3） | `192.168.1.180`（现侧系统） | 历史订单明细（2026-08-13），60 个订单 | **1,694 行** |
| `history_order_head2.sql`（上传件 4） | `192.168.1.180`（现侧系统） | 历史订单头（2025-05~2026-08） | **4,886 行** |

**数据覆盖期：2024-01-22 ~ 2026-08-13，共 927 个营业日。单店。**

> ⚠️ **导出件性质（搭模拟环境时实测，做数据分析前务必知悉）**
> · `history_order_head` 两份导出**有 4,886 行完全重复**（上传件 4 是上传件 2 的子集），
>   合并后须按全列去重，真实唯一行数 **88,616**，`(order_head_id, check_id)` 唯一
> · `history_order_detail` 是**抽样导出**：2026 批次 1,694 行只覆盖
>   `order_detail_id` 区间的 **76%**，少数订单缺行 → `SUM(actual_price)` 对不上
>   `original_amount`（剔除退菜行后 237/240 成立，3 个例外全落在缺行的单上）
> · `coolroid.sql` 的 `history_major_group` INSERT **被截断**（末行以逗号结尾无分号），
>   实际 1,368 行

> 📌 **主机说明**：`192.168.2.40` 为 POS 主库；`192.168.1.180` 为当前侧系统，**已替换** `192.168.58.128`。
> 主机地址属可变信息，**全部走配置文件，代码内不硬编码**（见 `00-架构总览.md` §2）。

---

## ⏳ 待确认事项（阻塞项以 🔴 标记）

| # | 事项 | 状态 | 影响文档 |
|---|---|---|---|
| 1 | 撤销操作的权限与时间窗（谁能撤销？限当班/24h内？） | 建议值已写入，待确认 | 03、04 |
| 3 | 只读账号能 `SELECT` 的表范围 | 待确认 | 02 |
| 4 | 2024-08 的 6 天数据丢失原因（是否会复发） | 待了解，降级预案已就位 | 01 §5.3、03 §10 |
| 6 | 小票上是否印有可扫的条码/QR | 非阻塞，可选优化 | 03 |

> **当前无 🔴 阻塞项，也无待决策的业务规则。** 剩余均为实施任务与运维背景信息。

### ✅ 已关闭的事项

| 事项 | 结论 |
|---|---|
| **10送1 核销时收银员的动作**（原 #2） | ✅ **模拟环境实测查明：加一条 `menu_item_id = -2` 的折扣行**，名称 `TARJETA 10+1`，`actual_price` 为负，对应头部 `discount_amount`。既非整单折扣也非改价 0。系统已据此自动识别并标记 `is_redeemed`，该单不计次不积分（`01` §3.1、`03` §5） |
| **`meal_item_rule` 初始化**（原 #5） | ✅ **已完成核对**：`price_1 ≥ 8.00` 共 61 项 = Bebida 32（整瓶红酒，非餐费项）+ Comida 22（BOX/COMBO，已入表）+ **Menú 7（套餐，已全部入表）**。规则表 41 条已覆盖全部需判断项，巡检实测「未归类 0 个」 |
| `major_group` / `family_group` 数据 | **已提供**。`major_group` = 1 Comida / 2 Bebida / **3 Menú** / 4 Postres；餐费项确定为 7 项，见 `01` §4.2 |
| `MENÚ DEL DIA` 规则 | **不参与十送一**（`counts_visit = 0`）；金额积分可后台开关 |
| **`BOX` / `COMBO`（22 项）** | ✅ **外卖产品线，三个开关全 `0`**（含 `earns_points = 0`，金额不计入积分）。堂食单中出现也按外卖处理不计入。实测 20 行全在外带订单，堂食 0 行（`04` §6.2.1） |
| 儿童套餐（`1490`/`1290`） | **后台可自由设置**是否参与十送一，默认参与。规则改为可维护表 `meal_item_rule`（`04` §6.2） |
| 计次口径 | **`by_portion` 已确认采用**：按 `SUM(quantity)` 计次；整单记一人 3 份 = +3 次，商业影响已知悉并接受 |
| AA 计次规则 | 由「按套餐份数计次」统一解决（`03` §3.2），AA 与混点场景自洽 |
| `192.168.1.180` 身份 | **侧系统**，已替换 `192.168.58.128`。单店，规则无需按店分化。IP 全部走配置文件 |
| 免费餐判据 | 双保险：价格字段判据（`01` §3.2）+ 餐费项规则表（`03` §5.3） |
| 营业日切点 | **已用 POS 自身数据验证** = 02:00，口径为 `original_amount`，324/332 天完全一致（`01` §5.2） |
| **`actual_price` 单价 vs 行小计** | ✅ **已用 5,000 行明细证实是「行小计」**（242/242 行满足 `actual_price = product_price × quantity`），金额直接用 `actual_price` 不再乘 `quantity`；份数仍用 `SUM(quantity)`（`01` §3.3） |
| 明细合计对应的头部字段 | ✅ **`SUM(actual_price) == original_amount`（折扣前），153/153 = 100%**。订单级折扣只在 `discount_amount`，故扣除须按比例，分母取 `original_amount`（`01` §3.3.1） |
| `actual_amount` 语义 | ✅ **收款额，含待找零**（172 行差额呈钞票面额模式）。积分基数用 `LEAST(should_amount, actual_amount)`（`01` §2.2） |
| 明细价格字段第五种组合 | ✅ **不存在**。2026 数据仅出现 3 种组合，均为已知类型 |
| **2026 年数据交叉复核** | ✅ **全部结论在 2025-05~2026-08 数据上复现**：`actual_price` 行小计 97/97、`SUM==original_amount` 57/58、`status` 恒 2（4,886 行）、`return_amount` 恒 0、`serial_id` 0 重复、恒等式 0 例外 |

### 价格字段组合复验 SQL（已用 2026 数据复核通过，此处保留供日后重跑）
```sql
-- 确认价格字段只有已知的 4 种组合，没有第五种
SELECT
  CASE WHEN product_price  > 0 THEN '>0' ELSE '=0'  END AS product_price,
  CASE WHEN original_price IS NULL THEN 'NULL'
       WHEN original_price > 0 THEN '>0' ELSE '=0'  END AS original_price,
  CASE WHEN actual_price   > 0 THEN '>0' ELSE '=0'  END AS actual_price,
  COUNT(*) AS cnt
FROM history_order_detail
WHERE order_time >= DATE_SUB(NOW(), INTERVAL 30 DAY)   -- 走 idx_order_time
  AND menu_item_id > 0
  AND condiment_belong_item = 0
GROUP BY 1,2,3;
```

> ⚠️ 这条查询会扫近 30 天明细（约 8 万行），**必须在 03:00–05:00 窗口执行**，且仅作为一次性复验，不进入常规任务。
