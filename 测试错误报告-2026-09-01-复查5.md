# 测试错误报告（第五次复查 · 只查「会不会让餐厅赔钱」）· 2026-09-01

> **本轮命题**（来自委托方）：**做了一次深度修复，看一下是否存在会造成餐厅经济损失的漏洞。**
> 所以这一轮只沿一条线打：**免费餐券能不能被凭空造出来、或者用远低于设计成本的代价换到。**
>
> **不改动仓库中任何既有文件**，涉及设计结论一律**引用** `docs/` 与源码原文。
>
> 前五轮：[`08-30`](./测试错误报告-2026-08-30.md)（16 条）·
> [`复查`](./测试错误报告-2026-08-31-复查.md)（+5）·
> [`复查2`](./测试错误报告-2026-08-31-复查2.md)（+3）·
> [`复查3`](./测试错误报告-2026-08-31-复查3.md)（+5）·
> [`复查4`](./测试错误报告-2026-08-31-复查4.md)（+3）
>
> **提交人**：网络系统测试员
> **被测版本**：`origin/main` @ `fa6e8ed`
> **本轮被复查的修复**：`2cce521`（复查4 三条）+ `baa0bf1`（底线漏洞 4 条）+ `4dc3285`（自查产钱链 3 条）
> **测试环境**：PHP 8.4.19 · MariaDB 10.11.14 · `php -S` 开 8 个 worker
> **POS 主库**：照 `pdb/pos_schema.sql` 重建的空表 + 手工夹具（**非真实门店数据**）

---

## 1. 一句话结论

**上一轮的两条硬伤都真修好了，实测确认。但这次新加的三道防线，每一道都能绕过去 —— 三条都是真的会让餐厅赔钱。**

| 编号 | 一句话 | 一顿免费餐的真实成本 | 有没有告警 |
|---|---|---|---|
| [M-01](#m-01) | 「计一次至少要分到 5 元」这道闸门**不随份数放大** | 设计 € 50 → 实际 **€ 20** | 🔴 一条都没有 |
| [M-02](#m-02) | 回溯补发的护栏只认两个键，**换口径、改等级都绕得过去** | 一次点击 → **12～37 张券** | 🔴 一条都没有 |
| [M-03](#m-03) | 手工录入日上限按**日历零点**切，而营业日切点是 02:00 | € 300 上限 → 一个班次录 **€ 600** | 🟡 券的那条响了 |

三条的共同点：**都不需要经理权限、不需要 override、普通收银员或普通后台操作就能做到。**
M-01 我直接用 `cashier1`（role=1）走真实 HTTP 打通了。

---

## 2. 基线

| 套件 | 上轮 | 本轮 | 结果 |
|---|---|---|---|
| `tests/run.php` | 967 | **986** | 全部通过 |
| `tests/smoke.php --fresh` | 537 | **598** | 全部通过 |
| `tests/http_sweep.php` | 82 | **82** | 全部通过 |

与三个提交里写的数字完全对得上。

---

## 3. 先说好消息：上一轮的两条硬伤，实测确认真的修好了

### 3.1 N5-01 并发死锁 —— ✅ 已修复

同一套压法（四台 Pad 交叉点人、三位会员、各 80 单），先 `FLUSH STATUS` 再跑：

```
Q1   成功 80 / 80
Q2   成功 80 / 80
Q3   成功 80 / 80
Q4   成功 80 / 80
──────────
Innodb_deadlocks    0
```

上一轮同样的压法是 **141 / 320 成功、179 次死锁**。现在 **320 / 320，死锁 0 次**。

`grantOne()` 里那句固定加锁顺序是真正解决问题的那一道；
`LocalDb::transaction()` 的 1213/1205 重放是第二道保险；
死锁归 `E110` 之后界面上也不再叫人去找管理员。三道都到位。

### 3.2 N5-02 撤销不收券 —— ✅ 已修复（两支都验了）

```
门槛 3 次。第 3 次记的是别人的桌，券已经发出、还没吃
  三次记完             计次 3 · 已发券计数 1 · 券：189F3AEE(状态1)
  撤销：{"ok":true,"reversal_id":2771}
  撤销后                计次 2 · 已发券计数 0 · 券：189F3AEE(状态4)

  ▸ 那张券还能不能吃掉？
     ★ 已经没有在手的券了 —— 撤销把它收回去了

  ▸ 客人后来自己吃到第 3 次，还发不发得出来？
     checkAndGrant → 发 1 张
  第 3 次（真实的） 计次 3 · 已发券计数 1 · 券：189F3AEE(状态4)，9DD2D184(状态1)
```

**两头都对了**：发错的那张收回去了（状态 4 = 作废），
客人后来真正挣到的那张也照常发得出来 —— 上一轮那个「永远算出 0」的坑填上了。

已核销的那一支也验了，`reward_on_reversed_grant` 告警照响：

```
撤销：{"ok":true} · 券 E0FAD60A(状态2) 保持不动
告警：reward_on_reversed_grant → 撤销了一笔记账，但由它带出的 1 张免费餐券
      【已经被核销】，收不回来了。请人工核对这位客人的奖励进度……
```

---

<a id="m-01"></a>
## 4. M-01 🔴 「计一次至少要分到 5 元」这道闸门不随份数放大

### 4.1 这道闸门是这次新加的，方向完全正确

`baa0bf1` 新增 `min_amount_per_visit`（出厂 € 5.00），
`ConfigSchema` 里把要防的事写得很清楚：

> ★ 不设的后果：一桌 71.70 三份套餐，让真正点套餐的人认领 71.69，
> 把剩下的 **0.01 连着 1 份**丢给同行没点套餐的人，那个人就白得一次进度

而 `PointsEngine` 里的判据是：

```php
if ($prt > 0 && $minPerVisitCents > 0 && $amt < $minPerVisitCents) {
    return ['ok' => false, 'error' => 'amount_too_small_for_visit', ...];
}
```

**`$amt` 是这一笔分配的总金额，`$prt` 是这一笔分配拿走的份数 —— 两个数没有相乘。**

而 `visitsFor()` 在 `by_portion` 口径下是：

```php
if ($mode === 'by_portion') {
    return $portions;      // 份数就是次数
}
```

于是这道闸门实际守的是「**每一笔分配** 至少 5 元」，
而它想守的是注释里写的「**要计一次**，分到的钱至少得够得上一份计次套餐的下限」。
在 `by_portion` 口径下，一笔分配可以买到 N 次 —— **门槛就被稀释了 N 倍**。

### 4.2 实测：用提交信息里的那张单，只把数字换一下

口径 `by_portion`，门槛「每 10 次送 1 张」，`min_amount_per_visit = 5.00`（全是出厂默认）。
那张 71.70 元、三份套餐的单：

```
① 提交信息里描述的那种玩法（0.01 连着 1 份丢给同行）—— 已经堵住
   结果：{"ok":false,"error":"amount_too_small_for_visit"}

② 同一张单，只把数字换一下：给同行 5.00 连着【三份全部】
   结果：{"ok":true,"error":null}
   A 真正付了 66.70 的那位 计次 0 · 累计消费 € 66.70
   B 只付 5.00 的同行     计次 3 · 累计消费 € 5.00
```

**闸门拦住了 0.01 换 1 次，没拦住 5.00 换 3 次。**
顺带看一眼 A：真正掏了 66.70 的那位客人，**一次都没记上**。

再跑三趟同样的操作：

```
③ B 再来三趟（每趟 5.00 拿走三份）
   → 第 3 趟之后发出 1 张免费餐券

   B 最终：计次 12 · 累计消费 € 20.00 · 在手券 1 张
        券 DF883DDC（发于进度 12）
   全程告警：（一条都没有）
```

| | 设计意图 | 实际 |
|---|---|---|
| 一张免费餐券的最低成本 | 10 次 × € 5.00 = **€ 50.00** | **€ 20.00** |
| 一次进度的最低成本 | € 5.00 | **€ 1.67** |

### 4.3 普通收银员就能做到 —— 走真实 HTTP 验过

不是只有服务层能构造。用 `cashier1`（role = 1，不是经理，没有任何 override）
直接打 `/api.php/points/grant`：

```
普通收银员登录：True  role=1
  ① 0.01 连 1 份（旧玩法）：ok= False  error= amount_too_small_for_visit
  ② 5.00 连 3 份（新玩法）：ok= True   error= None
```

### 4.4 影响范围要说清楚

**只在 `visit_count_mode = by_portion` 下成立。**
默认的 `once_per_period` 和 `by_order` 每笔分配最多换 1 次，门槛是准的。

但 `by_portion` 是 `ConfigSchema` 里正式列出的三个选项之一
（「按套餐份数（3 份 = 3 次）」），描述里说它是「买 N 份送 1 份」的老口径 ——
**从旧方案迁过来的门店正是会选它的那一类**。而这道闸门恰恰是为了
「同行蹭次数」这件事加的，在这个口径下它对同一个动作只是抬高了一点点门票价。

### 4.5 修的方向（供参考，我不改代码）

判据乘上份数即可 —— 它本来就叫「计**一次**至少要分到多少钱」：

```php
if ($prt > 0 && $minPerVisitCents > 0 && $amt < $minPerVisitCents * $prt) {
```

`once_per_period` / `by_order` 下一笔分配最多换 1 次，
乘不乘份数结果一样，所以这一改对默认口径**零影响**，
只把 `by_portion` 拉回注释里承诺的那条线。

---

<a id="m-02"></a>
## 5. M-02 🔴 回溯补发的护栏只认两个键，换口径、改等级都绕得过去

### 5.1 护栏本身是这次新加的，也确实管用

`4dc3285` 发现「门槛填 0 = 一位来过 10 次的会员当场发出 10 张」，
于是在保存配置时算出「这一下全店会补发多少张」并挂告警。实测这一条是好的：

```
════ 对照：把「10 次」调低到「8 次」—— 有护栏的那条路 ════
   {"key": "reward_threshold_visits", "value": "8", "will_issue": 1}
   告警：reward_threshold_lowered（severity 3）
```

### 5.2 但护栏是钉在两个键上的

```php
$thrKeys = ['reward_threshold_visits', 'reward_threshold_amount'];
$before  = in_array($key, $thrKeys, true) ? (float)$app->cfg()->get($key, '0') : null;
...
if ($before !== null && (float)$val > 0 && (float)$val < $before) { ... }
```

只要「应发张数」不是通过这两个键的数值变小而变大的，护栏就看不见。
**有两条路满足这个条件，我都实测通了。**

### 5.3 第一条：换积分口径（`reward_mode`）

店里跑的是「来 10 趟送 1 次」，而 `reward_threshold_amount`（€ 50）
早就填在那儿一直没启用 —— 这是很自然的状态，两个门槛的格子都在同一页上。
老板想试试「满 50 送一次」，把下拉框从「按次数」改成「按金额」：

```
════ 换一个键：把口径从「按次数」改成「按金额」（门槛 € 50 早就填好了）════
   {"key": "reward_mode", "value": "amount"}
   告警：（一条都没有）
   全店待补发（pendingAcrossMembers）= 37 张
```

**从 0 张跳到 37 张，界面上没有 `will_issue`，告警一条都没有。**
（我的夹具里只有 3 位会员。真实门店几百位会员，这个数字按比例放大。）

这 37 张不是纸面数字 —— 会员下一次来吃饭就当场兑现：

```
这位常客下一次来吃饭，收银员照常扫卡记账（一笔普通的 45.00）：
  记账前：计次 25 · 累计消费 € 1250.00 · 已发券 2 · 在手券 0
  记账后：计次 26 · 累计消费 € 1295.00 · 已发券 25 · 在手券 23

  ★ 这一笔 45.00 的普通记账，当场发出 23 张免费餐券
  告警：（一条都没有）
  全店剩余待补发：14 张（其余会员下次来各自领）
```

### 5.4 第二条：改等级门槛（`/tiers/save`）

`app/cp/routes.php:886` 的 `/tiers/save` 里，
**没有 `pendingAcrossMembers()`、没有 `will_issue`、没有告警**，只写了一条审计。

而等级门槛是真正决定发多少张的那个数 —— `checkAndGrant()` 用的是
`progressOf($m, $this->tiers->forMember($memberId))`：

```
会员挂在等级：{"code":"gold","threshold_visits":2,...}
按【他这张卡的等级】算：应发 15 / 已发 3 / 欠 12（门槛 2）
而后台改门槛时看的那个数（pendingAcrossMembers，按全局门槛）= 0

这位客人下次来，收银员照常记一笔 42.00：
  ★ 当场发出 12 张免费餐券（在手 12 张）
  告警：（一条都没有）
```

### 5.5 根子上是同一件事：护栏用的那个数看不见等级

```php
public function pendingAcrossMembers(): int
{
    $r = $this->rule(null);          // ← 全局规则，不看任何会员的等级
    ...
    $sql = '... FROM member WHERE store_code = ?';
}
```

`rule(null)` 是**全局**规则。所以：

1. `/tiers/save` 改等级门槛 → 真实发放会变，这个数**看不见** → 护栏不响（实测 0 → 0，实际 12 张）；
2. 即使走**有护栏的那条路**，对挂了低门槛等级的会员，
   界面上显示的 `will_issue` 也**比真实张数少** —— 经理是照着一个偏小的数在做决定。

### 5.6 修的方向（供参考）

护栏不该钉在「哪几个键」上，而该钉在「**这次保存会不会让应发张数变多**」上：
保存前后各算一次 `pendingAcrossMembers()`，变多就告警。
这样 `reward_mode`、`reward_enabled`、`/tiers/save` 自动都被罩住，以后再加新维度也不用改这里。
同时 `pendingAcrossMembers()` 要**按会员各自的等级**算，否则它自己就是偏的。

---

<a id="m-03"></a>
## 6. M-03 🟡 手工录入日上限按日历零点切，而营业日切点是 02:00

### 6.1 这道上限的设计意图写得很硬

```
★ 超了经理也不能放行 —— 能放行的话它就不是上限，
  而放行权本身就在最可能滥用的那个人手里。
```

出厂 € 300.00。但判断「今天录了多少」的那句 SQL 是：

```php
// app/lib/Repo/LedgerRepo.php:191
'... AND created_at >= ?',
[$this->storeCode, self::SRC_MANUAL, $operatorId, self::S_ACTIVE, date('Y-m-d 00:00:00')]
```

**`date('Y-m-d 00:00:00')` 是日历零点。而这套系统的营业日切点是 02:00**
（`business_day_cutoff`，`docs/03 §2`），晚市 19:30 做到凌晨 02:00 —— **一个班次是跨零点的**。

### 6.2 实测

把门店时区拨到本地时间 01:18（模拟晚市尾声，比等到半夜快一点，
代码只看本地挂钟与零点、02:00 的相对关系，行为完全一致）：

```
════ 单日手工录入上限 € 300.00　·　营业日切点 02:00 ════
  门店此刻本地时间：2026-09-02 01:18:07　—— 晚市 19:30 做到 02:00，这个班次还没下班

  22:40 那笔 → 营业日 2026-09-01
  此刻 01:18 → 营业日 2026-09-01
  ★ 同一个营业日、同一个班次、同一个收银员

  代码问的是「今天」录了多少 —— 而它按【日历零点】切：
    manualAmountToday() = € 0.00　（实际这个班次已录 € 300.00）

  零点过后，同一个收银员再录 300.00（普通收银员，没有经理放行）：
    结果：{"ok":true,"error":null,"detail":null}
    这一个班次手工录入合计：€ 600.00　（上限写的是 € 300.00）

  按金额发券（门槛 € 50）：这一个班次换到 12 张免费餐券（在手 12 张）
  告警：["reward_from_manual_entry"]
```

**同一个班次、同一个人、没有任何放行，录进去 € 600，上限写的是 € 300。**
换算成券：门槛 € 50 时是 **12 张**免费餐，而这道上限想限住的是 6 张。

### 6.3 一个附带的观察

`manualAmountToday()` 的条件里带 `status = S_ACTIVE`。
撤销一笔手工录入会把原流水的 status 改掉 —— 于是**撤销会把额度还回来**。
现在有了 `clawBackOverIssued()`，撤销时券会跟着收回，所以这条不再直接产券；
但作为一道「谁都过不去」的上限，它可以被 `录满 → 撤销 → 再录满` 反复重置。
建议连同 6.4 一起处理。

### 6.4 修的方向（供参考）

把起点从日历零点换成**当前营业日的起点**。
`BusinessDay` 已经有现成的区间方法（而且里面已经处理了夏令时那天 02:00 不存在的情况），
直接用它，不要再出现第二套「一天从几点开始」的算法 —— 这套系统里
`business_date`、餐期、告警统计都走营业日，只有这道上限走日历日。

---

## 7. 顺带复核过、确认没问题的

| 检查项 | 结论 |
|---|---|
| `grantMerged()` 能不能绕过 `min_amount_per_visit` | ✅ **不能**。合并把每张单**剩余的全部金额**给这位会员，金额天然与份数匹配，构造不出「小钱换多次」 |
| 撤销的幂等性 | ✅ 第二次 `already_reversed` |
| 撤销已核销的券 | ✅ 不动券、不减 `rewards_issued`，挂 `reward_on_reversed_grant` 告警 |
| 按金额发券时手工录入的占比风控 | ✅ `reward_from_manual_entry` 告警实测会响（M-03 那一轮就响了） |
| 死锁重试会不会重复记账 | ✅ 事务整个回滚后重放，`grant()` 的 POS 读取在事务之外，闭包内没有跨事务副作用 |
| 并发压测后的账实 | ✅ 320 单压完，会员余额与流水合计一致 |

一条**记下来但没有算作缺陷**的：`CardRepo::generateBatch()` 的事务闭包用
`use (…, &$out)` 按引用收集结果。现在 `transaction()` 会重放，
重放时 `$out` 里还留着上一次的行 —— 发卡批次的返回列表会出现重复。
发卡撞死锁的概率极低，也不涉及钱，但既然重放是新加的，这类
「闭包里带外部状态」的写法值得全仓扫一遍。

---

## 8. 一句话给老板

> 上一轮那两个会赔钱的坑真的堵上了，这次新修的方向也都对。
> 但**新加的三道闸门都还差最后一寸**：
> 一道**忘了乘份数**，一道**只认两个开关**，一道**把营业日当成日历日**。
> 三道都不需要经理权限就能绕过去，而且绕过去之后**基本听不到声音**。

---

## 9. 附：本轮用到的复现脚本

都在会话临时目录下，**没有进仓库**：

| 脚本 | 用途 |
|---|---|
| `m1b_setup.php` / `m1b_probe.php` | 71.70 三份套餐那张单：0.01 换 1 次 vs 5.00 换 3 次 |
| `m2_setup.php` / `m2_probe.php` | 换 `reward_mode` 前后的全店待补发，以及下一笔普通记账当场发出多少张 |
| `m3_probe.php` | 跨零点的同一个班次，手工录入日上限是否重置 |
| `m4_probe.php` | 改等级门槛之后，护栏看到的数 vs 真实发放张数 |
| `dl2_worker.php` | 四台 Pad 交叉点人并发压测（复验 N5-01） |
| `cb_probe.php` / `cb2_probe.php` | 撤销收券的两支（未核销 / 已核销），以及后续能否正常补发 |

---

*本报告只新增本文件，未改动仓库中任何既有文件。*
