# 测试错误报告（第八次复查 · R-01 验收 + 回归排查）· 2026-09-03

> **本轮命题**（来自委托方）：**做了一次全面修复，看一下是不是引入了新的错误。**
>
> **不改动仓库中任何既有文件**，涉及设计结论一律**引用** `docs/` 与源码原文。
>
> 前八轮：[`08-30`](./测试错误报告-2026-08-30.md)（16）·
> [`复查`](./测试错误报告-2026-08-31-复查.md)（+5）·
> [`复查2`](./测试错误报告-2026-08-31-复查2.md)（+3）·
> [`复查3`](./测试错误报告-2026-08-31-复查3.md)（+5）·
> [`复查4`](./测试错误报告-2026-08-31-复查4.md)（+3）·
> [`复查5`](./测试错误报告-2026-09-01-复查5.md)（+3）·
> [`复查6`](./测试错误报告-2026-09-02-复查6.md)（+1）·
> [`复查7`](./测试错误报告-2026-09-02-复查7.md)（+1）
>
> **被测版本**：`origin/main` @ `d1ce7fc`
> **本轮被复查的改动**：`18618fa`（R-01 + 同形状第二处）· `b78db7d`（渗透自查）·
> `6dcd029`（赠券专项）· `74a7d36`（随机化模型测试）· `a7b67a0`（白送与卡死专项）·
> `5968e61`（第五条白送）· `c494523`（cron 并发锁）
> **提交人**：网络系统测试员
> **测试环境**：PHP 8.4.19 · MariaDB 10.11.14 · `php -S` 开 8 个 worker
> **POS 主库**：照 `pdb/pos_schema.sql` 重建的空表 + 手工夹具（**非真实门店数据**）

---

## 1. 一句话结论

**R-01 修好了，顺带自查出来的那一大批我也没能推翻。但把同样的动作放到【两台 Pad 同时做】，那个「多退一次」又回来了。**

| | 结论 |
|---|---|
| R-01（上一轮我报的）是否修好 | ✅ **修好了**，走真实 HTTP 复验：第三步停在 0，不再是 −1 |
| 他们自己那套随机模型测试 | ✅ 我按三种计次口径 × 多个种子跑了约 **3,400 步**，一条不变量都没破 |
| **是否引入新错误** | 🔴 **一条**：[S-01](#s-01) 顺序执行修好了，**并发下同一个「多退一次」原样复现** |

S-01 一句话：`clawBackVisitOnRedeem`、`reverseInTx`、`applyShrink`
这次统一成了同一个公式 ——「看**还剩多少没退**」——
但三条路都是**先把账读出来、再去取会员行锁**，
而且谁也不锁对方的锚点。**公式统一了，加锁边界没统一。**

实测两处，都是 100% 复现：

| 并发组合 | 应当是 | 实测 |
|---|---|---|
| 核销 × 撤销（同一张单） | 计次 0 | **计次 −1**（60 / 60 轮） |
| 核销 × 核销（一单两张券，`by_portion`） | 计次 0 | **计次 −1**（40 / 40 轮） |

客人在界面上会看到：**「已累计 -1 次 / 每 3 次送 1 次，还差 4 次」**。

---

## 2. 基线

| 套件 | 上轮 | 本轮 | 结果 |
|---|---|---|---|
| `tests/run.php` | 1002 | **1012** | 全部通过 |
| `tests/smoke.php --fresh` | 701 | **748** | 全部通过 |
| `tests/http_sweep.php` | 93 | **93** | 全部通过 |
| `tests/fuzz.php`（新增） | — | **13 组 ≈ 3,400 步** | 全部不变量成立 |

---

## 3. R-01 验收：修好了

用上一轮**完全相同**的三步，走真实 HTTP：

```
① 记账（A、B 各 50.00 一份）： ok= True
   TK000H0001ABC  计次 1
   TK000H0002ABC  计次 1
② B 拿出券，核销到这一单上： ok= True
   TK000H0002ABC  计次 0        ← 这一步本来就是对的
③ 撤销 B 那一笔： ok= True
   TK000H0002ABC  计次 0        ← ★ 不再是 -1
```

`LedgerRepo::activeCompensationsOf()` 这个抽法是对的：把判据从
「当初记了多少」换成「还剩多少没退」，而且 `reverseInTx` 与
`applyShrink` 两条路共用 —— 上一轮我只点了一处，他们自己找出了第二处，
这一点比我做得多。

## 3.1 他们自己那套随机模型测试，我没能推翻

`tests/fuzz.php` + `fuzz_invariants.php` 这个思路（让机器随机组合操作，
每一步之后把十条不依赖被测算法的事实全查一遍）是这几轮里最有价值的一件事。
我按它自己注释里的要求，**三种计次口径都跑**，并且 stable / 非 stable 两档都跑：

```
once_per_period  seed=7/21/99   各 250 步  ✅
by_order         seed=7/21/99   各 250 步  ✅
by_portion       seed=7/21/99   各 250 步  ✅
非 stable（会随机改门槛/换口径）
once_per_period  seed=3/42      各 300 步  ✅
by_portion       seed=3/42      各 300 步  ✅
```

约 3,400 步，**一条不变量都没破**。这一轮我没能用它找出问题 ——
所以我换到了它**结构上够不到**的地方：并发（它是单进程顺序执行的）。

---

<a id="s-01"></a>
## 4. S-01 🔴 并发下「多退一次」原样回来

### 4.1 根因：公式统一了，加锁边界没统一

三条路现在都问同一个问题「还剩多少没退」，但都是**先读、后锁**：

```php
// PointsService::reverseInTx()
$comps = $this->ledger->activeCompensationsOf($ledgerId);   // ← ① 先把账读出来
foreach ($comps as $c) { … $visits += (int)$c['counted_visit']; }

$this->members->lockById((int)$orig['member_id']);          // ← ② 才去取会员行锁
```

而各自锁的东西还不一样：

| 路径 | 先锁什么 | 读什么来决定退多少 |
|---|---|---|
| `clawBackVisitOnRedeem`（核销） | 券行 → 会员行 | `activeBySerial()` + `coupon` 计数 |
| `reverseInTx`（撤销） | **流水锚点行** → 会员行 | `activeCompensationsOf()` |
| `applyShrink`（值比对缩水） | **订单行** → 会员行 | 同上 |

**谁也不锁对方的锚点。** 于是两条路都能在对方写库之前读到同一份旧账，
然后各退各的 —— 同一次被退两遍。

### 4.2 实测一：核销 × 撤销

两个进程按同一张时间表逐轮**同时**动手（每轮一位新会员、一张券、一张已记账的单）：

```
redeem   成功 60 / 60
reverse  成功 60 / 60
──────────
计次为负：60 / 60 轮
  TK000R0000ABC → -1
  TK000R0001ABC → -1
  …
```

抽一位看流水，形状和上一轮 R-01 一模一样：

```
id   entry_type  amount   counted_visit  status  reverses_id
432      1        60.00         1           2      NULL      ← 原始记账
497      2       -60.00        -1           1      432       ← 撤销：读到的补偿集是空的，全额退
498      2         0.00        -1           1      432       ← 核销的补偿：在撤销读完之后才落库
                                合计 = 1 - 1 - 1 = -1
```

### 4.3 实测二：核销 × 核销（一单两张券）

`by_portion` 口径，一位客人吃两份（计次 2），手上正好两张券，两张同时核销。
**正确结果应当是各退一次 → 计次 0**：

```
券c1 成功 40 / 40
券c2 成功 40 / 40
── 正确结果应当是：两张券各退一次 → 计次 0 ──
vc    members
-1      40
```

这一处比上一处更隐蔽：`$take = min($freed, $net)` 里的两个数
**来自不同的表**（`$net` 读 `point_ledger`、`$freed` 读 `coupon`），
并发时它们可能停在**互相不一致的时刻** ——
一方读到 `$freed = 2`（对方的券已提交）而 `$net` 还是 2（对方的补偿还没落库），
于是一口气退了 2，加上对方退的 1，一共退了 3。

顺带说明：`5968e61`「一桌两张券，第二张一份也扣不动」修的是**顺序执行**下的少扣；
并发下同一个场景现在是**多扣**。同一个入口，两个方向。

### 4.4 破了他们自己那套不变量里的两条

拿 `tests/fuzz_invariants.php` 的 `inv_all()` 去查这个并发跑出来的状态：

```
破了 2 类，共 120 条：
  ②单笔净额为负    ×60
  ③会员余额为负    ×60
```

而这两条正是他们这一轮**专门加上去**的通用不变量
（「一顿饭退回去最多归零，不可能倒欠」）。
不是不变量写得不对 —— 是 fuzz **结构上到不了这个状态**：它单进程、顺序执行。

### 4.5 账没记乱，是多退了

按追加式账本的正确读法（**全部流水**合计，不筛 `status`）核对：

```
mismatched_all_rows  0
会员表 -1  /  全部流水合计 -1
```

**完全一致。**所以这一条和 R-01 一样：不是记乱了，是**多退了一次**，
而且这一次是明明白白写在账上的 —— 事后能查，但没人会去查。

### 4.6 客人看到什么

```
会员 273：计次 -1 · 门槛 3
进度条上会写：「已累计 -1 次 / 每 3 次送 1 次，还差 4 次」
```

**「已累计 -1 次」**直接摆在客人面前，而「还差 4 次」比门槛本身还大。

### 4.7 触发条件要说清楚

我测到的 100%，是在**两个动作被安排在同一瞬间开始**的条件下 ——
每轮我让两个进程等到同一个时间戳再动手。
**我没有测真实门店里这件事多久发生一次**，那取决于两台 Pad 的操作间隔。

但有三点让它不能当成「理论上才会发生」：

1. **多 Pad 并发是这套系统的既定前提** —— `grantMerged` 里那句
   `sort($serials)` 和 `RewardService` 里那段并发注释都是为它写的；
2. **后果是静默且永久的** —— 客人的计次就那么少了一次，
   下一顿免费餐要多来一趟，而账面上一切正常；
3. **现有测试结构上抓不到** —— 三套加上 fuzz 全是单进程顺序执行。
   这一轮唯一真跑并发的是 `deadlock_worker.php` / `manual_cap_worker.php`，
   而它们测的是死锁与额度，不是这条。

### 4.8 修的方向（供参考，我不改代码）

跟他们上一轮修额度锁时用的是同一招 —— **先取锁，再读账**：

* 三条路在读「还剩多少没退」**之前**，先把会员行的 X 锁拿到手
  （`members->lockById()` 提到最前面）。会员行是三条路唯一都会碰的行，
  拿它当互斥点最省事，也不会像区间锁那样把正常记账圈进来
  （`docs/13 §3.4` 那三版修法的教训）。
* `$net` 与 `$freed` 必须在**同一个锁的保护下**读 ——
  现在它们来自两张表，并发时可以停在互相不一致的时刻。
* 测试那一侧：fuzz 现在是单进程的，建议加一个**并发档**
  （两三个进程跑同一份随机序列、每步之后同样跑 `inv_all`）。
  ②③ 两条不变量已经写好了，差的只是把它放到并发下去查。

---

## 5. 其余改动逐项撞过，没有发现回归

| 检查项 | 结论 |
|---|---|
| R-01 本体（核销后撤销） | ✅ 走真实 HTTP 复验，停在 0 |
| 随机模型测试（三种口径 × stable/非stable，≈3,400 步） | ✅ 一条不变量都没破 |
| `markRedeemedByApp` 改成按 `portions_gross` 重算 | ✅ 迁移 019 **有回填**（`portions_counted + 已核销券张数`），老库升级不会把净份数算错；`portions_gross = 0` 那一档退回旧写法，不会把净份数清零 |
| 新的手工额度互斥锁（迁移 018） | ✅ 上一轮压过：160 笔 0 死锁、€300 一分不多 |
| 全站 HTTP 探针 | ✅ 93 项全过 |

---

## 6. 一条环境上的说明（不是代码问题）

我第一次跑 `http_sweep` 时红了 2 项（`⑬ 读到当前门槛`）。
排查下来是**我自己的探针库被前几轮的脚本清空了** ——
`vip_live.sys_config` 只剩 2 行（种子是 47 行），`operator` 表也是空的。
把 `db/seeds/0*.sql` 重新灌进去之后 **93 项全过**。

记在这里是为了说明：那 2 条红**不是这次改动带来的**。
顺带一提，这也说明探针对「库里缺配置」这件事的报错还可以更直白一点 ——
现在的提示是「拿不到当前门槛」，而真实原因是种子没灌。

---

## 7. 一句话给老板

> **上一轮我报的那条修好了**，而且他们自己还找出了同一形状的第二处，
> 又搭了一套随机模型测试 —— 我按它的要求跑了三千多步，一条都没推翻，
> 这一套是这几轮里最值钱的东西。
>
> **但同样的动作换到两台 Pad 同时做，那个「多退一次」原样回来了。**
> 这次统一了三条路的**公式**，没统一它们的**加锁边界**：
> 都是先把账读出来、再去锁人。
> 客人会看到「已累计 **-1** 次」，而这件事账面上一切正常、没人会去查。

---

## 8. 附：本轮用到的复现脚本

都在会话临时目录下，**没有进仓库**：

| 脚本 | 用途 |
|---|---|
| `r1_setup.php` + curl | R-01 验收：走真实 HTTP 的三步复验 |
| `race_setup.php` / `race_worker.php` | **S-01 之一**：核销 × 撤销并发（60 轮） |
| `race2_setup.php` / `race2_worker.php` | **S-01 之二**：一单两张券同时核销（40 轮） |
| `inv_check.php` | 拿 `fuzz_invariants.php` 的 `inv_all()` 查并发跑出来的状态 |

---

*本报告只新增本文件，未改动仓库中任何既有文件。*
