# 测试错误报告（复查）· 2026-08-31

> **性质**：对 [`测试错误报告-2026-08-30.md`](./测试错误报告-2026-08-30.md) 所列 16 条的修复复查，
> 外加本轮新发现的缺陷。**不改动仓库中任何既有文件**，涉及设计结论一律**引用** `docs/` 原文。
>
> **提交人**：网络系统测试员
> **被测版本**：`origin/main` @ `e1e4a58`（修复提交 `ab19795`，相对上轮基线 `b3fdbd5`）
> **测试环境**：PHP 8.4.19 · MariaDB 10.11.14 · Node 22 · Chromium（Playwright）
> **POS 主库**：照 `pdb/pos_schema.sql` 重建的空表 + 手工夹具（**非真实门店数据**）

---

## 1. 一句话结论

**16 条全部动了手，14 条彻底修好，2 条只修了一半；另外新引入 5 个问题，其中 2 个比原来的更急。**

上轮报告说过「这四个 bug 都位于测试已经跑到的路径上，只是没有断言」。
这次的修复把断言补上了 —— 新增的 `smoke ㉖`（真并发发券，起多进程）
和 `http_sweep ⑩`（时间窗封顶）都是能真红的断言，这一点做得很扎实。

但同一类问题换了个位置又出现了：**新写的四段代码里，有三段的验证只停在「代码写对了」，
没有跑一遍「换到真实场景里还对不对」。** 下面五条新问题全部属于这一类。

---

## 2. 基线（修复后重跑）

| 套件 | 上轮 | 本轮 | 结果 |
|---|---|---|---|
| `tests/run.php` | 887 | **928** | 全部通过（无 `config.php` 时 923 + 明示跳过 5） |
| `tests/smoke.php --fresh` | 458 | **470** | 全部通过 |
| `tests/http_sweep.php` | 67 | **71** | 全部通过 |
| `tests/browser/*.mjs` | 未执行 | **391**（15/20 个文件） | 全部通过 |
| `tests/e2e_pos.php` | 未执行 | 未执行 | 需可达的真实 POS 主库 |

> 本轮补跑了上轮没跑的浏览器测试（装了 Playwright，用 `/opt/pw-browsers` 里的 Chromium）。
> 20 个文件里 15 个跑通、391 项全绿；余下 5 个
> （`assignlock` / `bindamount` / `locate` / `merge` / `visitcount`）
> 需要 `tests/sim/live_orders.json`，而它由 `tests/sim/inject_live.php` 从真实主库导出件生成，
> 本环境没有那批数据 —— **不是失败，是跑不了**。
> ⚠️ 巧的是这 5 个恰好覆盖「找单 → 分摊 → 合并 → 计次」这条链，
> 而本轮改动最大的正是这一段（见 [NEW-04](#new-04)）。

---

## 3. 上轮 16 条的复查结果

| 编号 | 结论 | 复验方式 |
|---|---|---|
| BUG-01 | ✅ **已修复** | 跑真 `bin/cron.php incremental`，已记账订单的 7 个字段一个不变 |
| BUG-02 | ✅ **已修复** | 6 进程对齐调用 `checkAndGrant()`，只发 1 张券 |
| BUG-03 | ✅ **已修复**（但无断言，见 [§5](#5-测试覆盖缺口)） | 真库 100.00 的单分四次，75/100 时状态正确为 1 |
| BUG-04 | ✅ **已修复** | 传 5256000 分钟被夹到 60 |
| BUG-05 | ✅ **已修复** | 999999.99 被 `exceeds_manual_hard_limit` 拒；自审留下 `review_accept_self` |
| BUG-06 | ✅ **已修复** | 4 位 PIN → 400 +「PIN 至少 6 位」；三个必填项分开报 |
| BUG-07 | ✅ **已修复**（但引入 [NEW-05](#new-05)） | `new CardNumber('VIP')` 构造即抛，消息清楚 |
| BUG-08 | ⚠️ **修了一半** | 核销模式已共用 ✓；**税额那一半无效**，见 [NEW-01](#new-01) |
| BUG-09 | ✅ **已修复** | 审计写的是 `order_free_meal` |
| BUG-10 | ✅ **已修复**（但引入 [NEW-02](#new-02)） | `checkGates()` 改用 `posNow()` |
| BUG-11 | ✅ **已修复** | 前跳日相邻窗口首尾相接，不再重叠 |
| BUG-12 | ✅ **已修复** | `splitEvenly()` 不再造出「0 元 N 份」，且 `[3,3,2,2]` 保持不变 |
| BUG-13 | ✅ **已修复** | 恒等的 `scale()` 换成直接相减，边界在注释里写明不处理 |
| BUG-14 | ⚠️ **修了一半** | 口径换了个轴，仍对不上，见 [NEW-03](#new-03) |
| BUG-15 | ✅ **已修复** | 未登记键被拒；`drink_major_group` 后台可见 |
| BUG-16 | ✅ **已修复** | 无 `config.php` 时明示「跳过 5 项」并说明原因 |

### 几处值得单独说的

**BUG-01 的修法是对的。** `upsert($o, bool $computed)` 把「POS 直接给的列」
和「要读明细才算得出来的列」分成两组，补抓路径只写前一组。
这比在 `storeOrder()` 里逐个 `COALESCE` 更稳 —— 加新列时只需要想清楚它属于哪一组，
而那句话就写在方法注释里。实测：

```
② 记账后        total=71.70  excl=18.30  tax=8.18  份数=3  已分配=71.70
③ cron 跑一轮后 total=71.70  excl=18.30  tax=8.18  份数=3  已分配=71.70
```

**BUG-02 的断言值得表扬。** `tests/reward_worker.php` 是单独一个进程文件，
由 `smoke ㉖` 起多份并对齐时刻 —— 因为「单进程顺序调两次」走的是另一条路
（第二次 `pending` 已经是 0），永远绿。这个判断是准的。

**BUG-12 顺带改了语义。** `splitEvenly()` 现在只在「分到钱的人」之间摊份数，
于是 `splitEvenly(2分, 3份, 5人)` 从 `[2/1, 0/1, 0/1, 0/0, 0/0]` 变成 `[2/3, 0/0, …]`。
份数守恒与 `[3,3,2,2]` 那条断言都没受影响，正常 AA 行为一致。

---

## 4. 本轮新发现

| 编号 | 级别 | 摘要 | 位置 | 引用文档 |
|---|---|---|---|---|
| [NEW-01](#new-01) | 🟠 中 | 值比对读了一个没取回来的列 → 税额修复无效，冲正少退、会员多留分 | `ReconcileService.php:160`<br>`OrderRepo.php` `pendingVerify()` | `docs/01` §2.11 |
| [NEW-02](#new-02) | 🟠 中 | `grant()` 现在会打 POS，且在循环里 —— 违反类注释立的「不碰 POS」 | `PointsService.php:833` | `docs/02` §2、`docs/03` §10 |
| [NEW-05](#new-05) | 🟠 中 | 升级回归：已填 `card_prefix=VIP` 的门店升上来后收银员登不进 | `CardNumber.php` · `bin/init.php` | `docs/07`、`docs/10` |
| [NEW-03](#new-03) | 🟡 低 | 首页两个「今天」换了个轴，仍然对不上 | `cp/routes.php` `/dashboard` | `docs/01` §5.2 |
| [NEW-04](#new-04) | 🟡 低 | 「+ 添加会员」预置 1 份、份数框只读 → 点选菜品模式下整笔被拒 | `pad.js` `addPerson()` | `docs/03` §13 |

---

<a id="new-01"></a>
### NEW-01 · 🟠 中 · 值比对读了一个 SQL 没取回来的列 → 税额修复无效

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/Service/ReconcileService.php:160`（读）<br>`app/lib/Repo/OrderRepo.php` `pendingVerify()`（没取） |
| **触发条件** | `points_include_tax = 0` 且订单结账后金额被改（`docs/01` §3.4 实测约 2.9%） |
| **引用文档** | `docs/01` §2.11 税额与小票 SubTotal 吻合 |

**根因**

BUG-08 的修复补上了 `taxCents`：

```php
$taxCents = $this->cfg->get('points_include_tax', '1') === '1'
    ? 0
    : (int)($o['tax_amount'] !== null ? Money::toCents((string)$o['tax_amount']) : 0);
```

但 `$o` 来自 `OrderRepo::pendingVerify()`，它的 SELECT 列表是：

```sql
SELECT serial_id, order_head_id, check_ids, original_amount, should_amount,
       actual_amount, total_amount, allocated_amount
  FROM pos_order
```

**没有 `tax_amount`。** PHP 8 下 `$o['tax_amount']` 抛 `Undefined array key` 警告并求值为 `null`，
于是三元表达式恒走 `: 0` —— **`taxCents` 永远是 0，这一半的修复没有生效。**

**实测**

```
  pendingVerify() 取回的列：
    serial_id, order_head_id, check_ids, original_amount, should_amount,
    actual_amount, total_amount, allocated_amount
    含 tax_amount？ ❌ 否

  捕获到的 PHP 告警：
    ⚠ Undefined array key "tax_amount"  @ ReconcileService.php:160
```

**影响 —— 不只是告警噪音，会少退分**

`points_include_tax = 0`，小票 100.00、税 9.09（不含税 90.91）：

```
  ① locate → 可积分总额 total = 90.91  （= 100.00 − 9.09，对）
  ② 整单记账 → 成功，积分 90
  ③ POS 侧退掉一份套餐：100.00 → 75.00（税 6.82 → 不含税 68.18）
  ④ 值比对 → total=75.00  已分配=75.00  verify_status=2
     告警：订单 2608300200 金额由 € 90.91 缩至 € 75.00，已自动冲正 € 15.91

  正确应缩到 68.18（75.00 − 6.82），冲正 22.73
```

账本：

| id | entry_type | amount | points |
|---|---|---|---|
| 4 | 1（消费） | 90.91 | 90 |
| 5 | 3（退单冲正） | −15.91 | −16 |

净得 **74 分**，正确应为 **68 分** —— **多留了 6 分，正好是税额那一截**。
每一张被改过金额的单都会多留，比例等于税率。

**为什么没被测出来**

`smoke` 的值比对场景（⑩ 金额缩水）跑在 `points_include_tax = 1`（默认）下，
那时 `taxCents` 本来就该是 0，走哪条路结果都一样。
这一条只有把开关拨到 0 才现形，而没有任何一条断言拨过它。

**建议**

`pendingVerify()` 的 SELECT 加上 `tax_amount`；顺手把那个三元表达式写成
`Money::toCents((string)($o['tax_amount'] ?? '0'))` —— 少一个键就静默变成 0，
这种写法本身就不该出现在算钱的路径上。

**应补断言**：把 `points_include_tax` 设为 0 跑一遍 `smoke` 的值比对场景，
断言冲正后的 `total_amount` 是不含税值。

---

<a id="new-02"></a>
### NEW-02 · 🟠 中 · `grant()` 现在会打 POS，而且在循环里

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/Service/PointsService.php:833`（`checkGates()` 的补记时限循环内） |
| **触发条件** | 每一次记账；多桌合并时按单数倍增 |
| **引用文档** | `docs/02` §2 时间基准取自主库；`docs/03` §10 降级路径 |

**根因**

BUG-10 的修法是对的 —— 补记时限确实该用主库时间。但落点放错了位置：

```php
foreach ($serialIds as $sid) {
    $o = $this->orders->findBySerial($sid);
    if ($o === null) { continue; }
    $age = (strtotime($this->posNow()) - strtotime((string)$o['order_end_time'])) / 60;
```

`posNow()` 在**循环体内**，每张单打一次 POS；而 `checkGates()` 是在
`grant()` / `grantMerged()` 的**本地库事务里**跑的。

这与 `PointsService` 自己的类注释直接冲突：

> `grant()` 【不碰 POS】，只在本地库事务内完成分配，
> 因此主库抖动不会阻塞收银流程。

**实测 ①：POS 正常时，每张单一次往返**

（开 MariaDB `general_log`，在 grant 前后打标记，只数 Execute）

```
    单桌 grant()（1 张单）       → 1 次 POS 往返
    多桌 grantMerged()（2 张单） → 2 次 POS 往返
```

一个在单次请求内恒定的值，被查了 N 次。

**实测 ②：POS 卡住时，收银台真的会卡住**

现场最常见的抖动形态不是「连不上」而是「连得上、不应答」。
把 `pos_db` 指向一个接受连接但从不发 MySQL 握手的端口
（`read_timeout = 5`，来自 `config.example.php`）：

```
  单桌 grant()（1 张单）        耗时  5.01 秒 → 失败 manager_required
  多桌 grantMerged()（2 张单）  耗时 10.01 秒 → 失败 manager_required
```

`merge_max_orders` 默认 8 → 同样情形下最坏约 **40 秒**，
且全程占着一个已经开着的本地库事务。

**顺带一个更隐蔽的**：`posNow()` 在 POS 不可达时回落到 `date()`（应用服务器本地时间）。
于是补记时限这道闸门的**时间基准会随 POS 可用性来回切换** ——
两台机器的时钟或时区只要有差，POS 一掉线，全店记账就会突然开始要经理放行
（或者反过来，闸门突然失效）。这比一直用同一个错基准更难查。
本环境实测差 2 小时（容器 MySQL 走 UTC、PHP 走 Europe/Madrid），
`age` 从 5 分钟变成 125 分钟，每一单都撞闸门 —— 真实门店两台机器同在 Madrid 时不会这样，
**但这正说明这条路径对时钟差没有任何抵抗力**。

**建议**

`posNow()` 提到循环外、也提到事务外，一次请求只取一次；
或者更稳妥：在 `grant()` 进入事务【之前】取好时间戳传进去，
让类注释里那句「不碰 POS」重新成立。

**应补断言**：`smoke` 里把 `pos` 换成一个会抛 `PosUnavailable` 的假实现，
断言 `grant()` 仍在毫秒级返回。

---

<a id="new-05"></a>
### NEW-05 · 🟠 中 · 升级回归：已填 `card_prefix=VIP` 的门店升上来后收银员登不进

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/CardNumber.php` 构造函数（新增的拒绝）· `bin/init.php`（没查） |
| **触发条件** | 现有 `config.php` 的 `card_prefix` 含 I/L/O/U，然后升级到修复版 |
| **引用文档** | `docs/07` 装机手册；`docs/10` 上线步骤 |

**根因**

BUG-07 的修法（构造函数直接拒绝含 I/L/O/U 的前缀）本身是对的，
理由也写得很清楚 —— 归一化前缀会让 `VIP` 印成 `V1P`，不如不许用。

问题在**升级路径**。BUG-07 描述的场景就是「门店已经填了 VIP」。
这样的门店升上来之后：

```
  /api.php/health     → {"ok":true, "local_db":true, …}         ← 说一切正常
  /api.php/auth/login → {"ok":false,"error":"server_error",
                         "message":"系统内部错误（错误代码 E302-BD930F）"}
  首页 index.php      → HTTP 200                                 ← 页面打得开
```

`/auth/login` 的响应里要带 `$padSettings()`，那里会构造 `CardService` → `CardNumber` → 抛异常。
于是**页面打得开、健康检查说没事、收银员就是登不进去**，
唯一的线索是一句「系统内部错误」加一个 E302。
`/auth/me` 同理，已经登录的平板下一次心跳也会掉线。

**更要紧的是排查入口指错了地方**：

```
  card_prefix=VIP 时 bin/init.php repair 的结论：
    结论
      ✓ 全部就绪 —— 后台应该能进，套餐份数应该能算出来

  对照 bin/diag.php：
      ✗ config.card_prefix = VIP —— 不能含 I / L / O / U
      发现 1 个问题，按上面的提示处理
```

`diag.php` 查得出（新增的那段写得很好，连原因带修法都说清楚了），
**但 `init.php repair` 没有这一项，而 README 写的第一条排查命令是 `repair`**：

> ```
> php bin/init.php repair              # 环境与配置，一次查完并尽量自修
> ```

照着 README 走的人会看到「全部就绪」，然后店里登不进去。

**影响对比**：修复前，`VIP` 前缀只是让**发卡/查卡**这一块失效，积分照常记；
修复后变成**整个 Pad 停摆**。对已经踩了这个坑的门店，
这是把局部故障升级成了全面故障。

**建议**

* 把 `diag.php` 里那段前缀检查**也放进 `init.php repair`** —— 它是文档指定的第一入口；
* 或者让这个异常在 `padSettings()` 这条路上降级：前缀非法时不构造 `CardNumber`，
  把 `settings` 里的实体卡相关项置空并附一条明确的提示，
  让收银员至少还能记积分（与 `docs/03` §10「不阻塞收银流程」的一贯取舍一致）。

---

<a id="new-03"></a>
### NEW-03 · 🟡 低 · 首页两个「今天」换了个轴，仍然对不上

| 项 | 内容 |
|---|---|
| **位置** | `app/cp/routes.php` `/dashboard` |
| **引用文档** | `docs/01` §5.2 营业日切点 |

**根因**

BUG-14 的修复把 `granted_today` 从「自然日 + `created_at`」改成
「**订单的** `business_date`」，而 `points_today` 改成
「营业日窗口 + **流水的** `created_at`」。两个数现在按**不同的轴**计。

**实测**（客人拿三天前的小票来补记 —— `invoice_lookup_max_days` 默认 7 天，是系统明确允许的路径）

```
  首页 granted_today（记了几笔）= 0   ← 按【订单的营业日】数
  首页 points_today  （发了几分）= 40 ← 按【流水的写入时刻】数

  → 屏幕上会显示「今天记账 0 笔、发出 40 分」
```

修复前两者都按 `created_at`，彼此一致但与 `orders_today` 不一致；
现在 `granted_today` 与 `orders_today` 一致了，却与 `points_today` 不一致。
而「笔数」和「分数」恰恰是经理最容易横着对一眼的两个数。

**建议**：两者都按同一个轴。既然 `points_today` 的语义是「今天实际发出去多少分」，
那 `granted_today` 也该是「今天记了几笔」= 按 `created_at` 落在营业日窗口内计；
`orders_today` 是订单维度，本来就该按 `business_date`，标签写清楚即可。

---

<a id="new-04"></a>
### NEW-04 · 🟡 低 · 「+ 添加会员」预置 1 份且份数框只读 → 点选菜品模式下整笔被拒

| 项 | 内容 |
|---|---|
| **位置** | `wwwroot/assets/pad.js` `addPerson()` 与 `renderPeople()` |
| **触发条件** | `visit_count_mode = once_per_period`（默认）+ 点选菜品模式 + 点「+ 添加会员」 |
| **引用文档** | `docs/03` §13 计次口径 |

**根因**

新的 `addPerson()` 给新增的一位预置 `portions = portions_per_person`（默认 1）：

```js
const fixed = (S.order || {}).portions_per_person;
S.people.push({ member: null, amountCents: 0, portions: (fixed == null) ? 0 : Number(fixed) || 0 });
```

理由写得有道理（不预置的话那位永远拿不到次数，而框是锁的、他改不了）。
但**点选菜品模式**下，一位客人的金额来自他认领的菜，新增时必然是 0 ——
于是新增的这一位就是「金额 0、份数 1」，正是服务端硬拒的 `portions_without_amount`。
而份数框在 `lockPort || S.mode === 3` 下是 `readonly`，**收银员改不掉**。

**实测**（真浏览器，跑的是线上那份 `pad.js`）

```
  点选菜品模式，第 1 位认领了两份套餐：
    添加会员前： [{"amt":4780,"prt":2}]
    添加会员后： [{"amt":4780,"prt":2},{"amt":0,"prt":1}]
    份数输入框 readOnly： [true,true]
  ❌ 新增的这一位：金额 0、份数 1 —— 正是服务端硬拒的 portions_without_amount
```

**影响**

按 `PointsEngine::validateAllocations` 的规则，
「一笔里混进 0 元的人 → **整笔拒绝**」。所以只要服务员加了人却还没给他点菜就提交，
**整单都记不上**，包括已经点好菜的那几位。

可恢复（给他点一道菜会触发 `recomputePicks()` 清掉，或者删掉那一行），
错误提示「请把他那份餐费也分给他」也说得清楚 —— 所以定为低。
但这是柜台上一个客人正站着的时刻。

**顺带**：AA 模式下同样会预置 1 份，只是那边金额框可编辑，服务员填个数就过去了。

**建议**：`addPerson()` 里按模式判断 —— 点选菜品模式下预置 0（份数本来就由认领的菜决定，
`recomputePicks()` 会算），或者干脆在 `addPerson()` 之后调一次 `recomputePicks()`。

**应补断言**：这条属于「只有真浏览器能测」的那一类，
`tests/browser/` 下正好有 `assignlock.mjs` 覆盖这一屏。

---

## 5. 测试覆盖缺口

**BUG-03 的修复没有断言。** `applyAllocation()` 现在依赖
「MySQL/MariaDB 的 UPDATE 赋值从左到右求值、后面看到前面的新值」这条**方言级**的行为，
而 `smoke.php` 关于 `alloc_status` 的断言仍然只有两条：

```
  419: eq(2, alloc_status, '订单状态 = 已全额分配')
  455: eq(0, alloc_status, '订单状态回到未分配')
```

**中间的部分分配（也就是那个 bug 本身）依然没有断言。**
将来谁把 SET 子句换个顺序（把 `alloc_status` 挪到 `allocated_amount` 前面），
bug 会一字不差地回来，而测试全绿。

建议补一条：`total=100.00` 的单分配 25.00 后断言 `alloc_status === 1`。

---

## 6. 建议的处理顺序

| 顺序 | 编号 | 理由 |
|---|---|---|
| 1 | NEW-05 | 会让踩过 BUG-07 的门店在升级当天全店登不进；改动只是把 diag 那段搬进 repair |
| 2 | NEW-01 | 一行 SQL 漏了一列，导致 BUG-08 的一半没生效，且会静默多留分 |
| 3 | NEW-02 | 违反了类注释立的「grant() 不碰 POS」；POS 抖动时收银台真的会卡 5–40 秒 |
| 4 | §5 | BUG-03 的断言 —— 现在的修复没有任何东西守着 |
| 5 | NEW-04 · NEW-03 | 界面与报表层面，可合并处理 |

---

## 7. 本轮未覆盖的范围

* **`tests/e2e_pos.php`（95 项）未执行** —— 需可达的真实 POS 主库。
* **`tests/browser/` 有 5 个文件未执行** —— 需 `tests/sim/live_orders.json`
  （由 `tests/sim/inject_live.php` 从真实主库导出件生成）。
  这 5 个覆盖的是找单/分摊/合并/计次这条链，正是本轮改动最集中的地方。
* **真实 POS 数据分布未复验** —— `docs/01` 中基于 88,616 行真实数据的结论
  （歧义率、金额恒等式、`status` 恒为 2 等）本轮未复验，**仍以 `docs/01` 原文为准**。
* **未做压力测试** —— NEW-02 的耗时数据来自单次受控实验，不是性能评估。
* **未审查 `apk/` 容器侧**（不在本仓库内）。
* NEW-02 里那条「时钟基准随 POS 可用性切换」在本环境放大了（容器 MySQL 走 UTC、
  PHP 走 Europe/Madrid，差 2 小时）。真实门店两台机器同在一个时区时不会有这个放大，
  **但那条路径对时钟差没有抵抗力这一点是成立的**。

---

*报告完 · 全部实测数据由 PHP 8.4.19 + MariaDB 10.11.14 + Chromium 环境产出。*
