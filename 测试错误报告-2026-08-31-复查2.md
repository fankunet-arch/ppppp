# 测试错误报告（第二次复查）· 2026-08-31

> **性质**：对上一轮 5 条新问题 + 1 个测试缺口的修复复查，外加本轮新发现。
> **不改动仓库中任何既有文件**，涉及设计结论一律**引用** `docs/` 原文。
>
> 前两轮：[`测试错误报告-2026-08-30.md`](./测试错误报告-2026-08-30.md)（16 条）·
> [`测试错误报告-2026-08-31-复查.md`](./测试错误报告-2026-08-31-复查.md)（复查 + 5 条新问题）
>
> **提交人**：网络系统测试员
> **被测版本**：`origin/main` @ `9130524`（修复提交 `e42d725`，上轮基线 `e1e4a58`）
> **测试环境**：PHP 8.4.19 · MariaDB 10.11.14 · Node 22 · Chromium（Playwright）
> **POS 主库**：照 `pdb/pos_schema.sql` 重建的空表 + 手工夹具（**非真实门店数据**）

---

## 1. 一句话结论

**5 条修好 4 条，1 条只修了一半；测试缺口补上了，而且实测挡得住回归。另发现 2 个问题、1 条假绿断言。**

这一轮的质量比上一轮高一档：`pos_clock_offset_sec` 那个设计（Cron 顺手记时钟偏差、
记账路径一次 POS 都不打）是**真正解决了问题**而不是绕开它，
`tests/ExplodingPos.php`（一碰就抛的假 POS）也是把「不碰 POS」这句承诺
钉成断言的正确做法 —— 这种「什么都不做」的不变量很难测得可信，这个思路是对的。

剩下的问题只有一个模式：**NEW-05 的降级只做到了「登录能进」，就停在那儿了**，
而没有沿着收银流程再走一步。结果是收银员登得进去、一单也记不了，
而 `bin/init.php repair` 的提示语还写着「积分照常记」。

---

## 2. 基线（修复后重跑）

| 套件 | 上轮 | 本轮 | 结果 |
|---|---|---|---|
| `tests/run.php` | 928 | **931** | 全部通过 |
| `tests/smoke.php --fresh` | 470 | **486** | 全部通过（⚠️ 见 [R3-03](#r3-03)：02:00–07:00 之间跑必红） |
| `tests/http_sweep.php` | 71 | **75** | 全部通过 |
| `tests/browser/*.mjs` | 391（15/20 文件） | **391**（15/20 文件） | 全部通过 |
| `tests/e2e_pos.php` | 未执行 | 未执行 | 需可达的真实 POS 主库 |

---

## 3. 上轮 5 条 + 1 缺口的复查

| 编号 | 结论 | 复验方式 |
|---|---|---|
| NEW-01 值比对少取了一列 | ✅ **已修复** | 真库跑值比对，`total` 68.18 正确，PHP 告警消失 |
| NEW-02 `grant()` 打 POS | ✅ **已修复** | general_log 数出 0 次往返；POS 卡住时 5.01 秒 → **0.01 秒** |
| NEW-03 首页两个「今天」 | ✅ **已修复** | 真接口：补记场景下「1 笔 / 40 分」对上了 |
| NEW-04 加会员预置 1 份 | ✅ **已修复** | 真浏览器：新增会员为 `{amt:0, prt:0}` |
| NEW-05 升级后登不进 | ⚠️ **只修了一半** | 登录能进了，但**一单也记不了** → [R3-01](#r3-01) |
| BUG-03 的断言缺口 | ✅ **已补上** | 把 `applyAllocation` 改回旧写法，smoke 立刻从 486 全绿变成失败 1 项 |

### 逐条实测

**NEW-01** —— `reloadAmounts()` 现在一并回读 `tax_amount`，`verifyOne()` 用的是
「当下的」税额而不是镜像里那份旧值。修复注释里把三次踩坑（没传 → 读了没取的列 →
读了旧值）都记了下来，这份记录本身有价值。

```
  points_include_tax = 0；小票 100.00、税 9.09 → 不含税 90.91
  ① locate → total = 90.91
  ② 记账 → 积分 90
  ③ POS 侧退掉一份：100.00 → 75.00（税 6.82 → 不含税 68.18）
  ④ 值比对后 total = 68.18（正确应为 68.18）  verify_status=2
  PHP 告警：（无）
```

> 账本净得 67 分而非 68 —— 那是 `applyShrink()` 里「冲正积分向上取整（对商家有利）」
> 这条既定规则（`docs/03` §6.4）造成的 1 分差，**不是缺陷**。

**NEW-02** —— 换了个思路：不再在记账时问 POS，而是让 Cron
（本来每 20 分钟就要问一次 POS 时间）顺手把时钟偏差记进 `sys_config`，
记账时用「本机时间 + 偏差」。实测整条链走得通：

```
  ① Cron 跑之前，库里的偏差：（未记录）
  ② 本机 2026-08-31 09:00:26   POS 2026-08-31 07:00:26   差 -7200 秒
  ③ php bin/cron.php incremental -v
     [09:00:26] POS 时钟偏差更新为 -7200 秒
  ④ Cron 之后库里的偏差：-7200
  ⑤ posNow() = 2026-08-31 07:00:26   POS 真实 = 2026-08-31 07:00:26   偏差 +0 秒 ✓
  ⑥ pos_clock_skew 告警：（无，符合预期）
```

记账路径的 POS 往返数与耗时：

```
    单桌 grant()（1 张单）       → 0 次 POS 往返   （上轮：1 次）
    多桌 grantMerged()（2 张单） → 0 次 POS 往返   （上轮：2 次）

  POS「接得上、不应答」时：
    单桌 grant()        耗时 0.01 秒   （上轮：5.01 秒）
    多桌 grantMerged()  耗时 0.00 秒   （上轮：10.01 秒）
```

**NEW-04** —— 真浏览器跑线上那份 `pad.js`：

```
  点选菜品模式，第 1 位认领了两份套餐：
    添加会员前： [{"amt":4780,"prt":1}]
    添加会员后： [{"amt":4780,"prt":1},{"amt":0,"prt":0}]   ← 不再是 {0,1}
  ✓ 没有出现「0 元带份数」
```

顺带把 `recomputePicks()` 也封到每人 1 份（`prt` 从 2 变成 1），
于是同一位客人在「点选菜品」和「均摊 AA」两屏显示一致 —— 这一条上轮没提，是主动补的。

**BUG-03 的断言** —— 我把 `applyAllocation()` 改回旧写法（`allocated_amount + ?`）
再跑一遍 smoke：**486 项里立刻红 1 项**。断言确实挡得住。

---

## 4. 本轮新发现

| 编号 | 级别 | 摘要 | 位置 |
|---|---|---|---|
| [R3-01](#r3-01) | 🟠 中 | 卡片降级只做到「登录能进」，收银流程全部 500；`repair` 的提示语与事实相反 | `app/lib/App.php` `points()` · `bin/init.php` |
| [R3-02](#r3-02) | 🟡 低 | `pos_clock_offset_sec` 的 `readonly` 是装饰性的，且类型表达不了负数 —— 能改坏、改不回来 | `ConfigSchema.php` · `cp/routes.php` · `cp.js` |
| [R3-03](#r3-03) | 🟡 低（测试） | smoke 有一条断言依赖运行时刻，本地 02:00–07:00 之间必然失败 | `tests/smoke.php:2030` |

---

<a id="r3-01"></a>
### R3-01 · 🟠 中 · 降级只做到「登录能进」，收银流程全部 500

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/App.php` `points()` 的构造参数 · `bin/init.php` 的提示语 |
| **触发条件** | `config.php` 的 `card_prefix` 含 I/L/O/U（也就是 NEW-05 的同一场景） |
| **引用文档** | `docs/03` §10「不阻塞收银流程」 |

**修好的部分**

上轮 NEW-05 的两个要害都处理了，而且处理得对：

* `bin/init.php repair` 现在会拦下来，提示里连怎么改都写了 ——
  这是最重要的一条，因为 README 指的第一条排查命令就是它；
* `$padSettings()` 里包了 `try/catch`，登录不再被拖下水，
  还多带了 `cards_ok` / `cards_error` 两个字段让 Pad 知道卡功能关着。

**没修好的部分**

降级只做到了登录这一步。`App::points()` 的构造参数里同样带着 `$this->cardNumber()`：

```php
public function points(): PointsService
{
    return $this->once('points', fn() => new PointsService(
        …,
        $this->cardNumber(),      // ← 前缀非法时，构造即抛
    ));
}
```

于是**每一个走 `points()` 的接口都 500**：

```
  card_prefix = VIP 时，收银流程逐个接口：

  按桌号找单    /api.php/order/locate          ❌ 500 server_error（E302）
  按小票号找单  /api.php/order/locate-invoice   ❌ 500 server_error（E302）
  记账          /api.php/points/grant          ❌ 500 server_error（E302）
  手工录入      /api.php/points/manual         ❌ 500 server_error（E302）
  查会员        /api.php/member/search         ✓ 可用

  /auth/login → ✓ 登录成功 | cards_ok = false
```

也就是说：**收银员登得进去，然后一单也记不了。**
从收银台的角度看，与修复前的「登不进去」没有实质差别 ——
只是把失败点往后挪了一步，还多了一次「看起来登录成功了」的误导。

**而诊断工具现在会明确地说错话**

`bin/init.php repair` 的提示原文：

```
  ✗ config.card_prefix = VIP —— 不能含 I / L / O / U
     这几个字母在卡面上与 1 / 0 / V 分不清，扫码纠错会把它们换掉，
     卡号跟自己对不上：实体卡的查卡/建卡/激活全部停用。
     （积分照常记 —— Pad 不会因此登不进去，但卡片功能是关着的）
```

最后那一句 **「积分照常记」是错的**。修复前 repair 不查这一项、什么都不说；
现在它查了，但给出的判断与事实相反 —— 对着这句话的人会以为可以先上线、
回头再改前缀，而实际上店里一单都记不了。

**为什么没被测出来**

新增的 `http_sweep ⑪` 自己写明了：

> 这里只能验「正常配置下这两个字段确实回来了」——
> 真正的降级路径要改 config.php 才测得到

判断是准的，但被跳过的那一半恰好就是还坏着的那一半。

**建议**

`PointsService` 对 `CardNumber` 的依赖只用于在 `buildContext()` 里
format `existing_ledger` 的卡号（`docs/03` 那条「同一张卡在两处不能长得不一样」）。
改成可空注入（`?CardNumber`，为 null 时原样输出卡号）即可 ——
积分路径本来就不需要卡号格式化。同时把 repair 的那句提示改成实情。

**应补断言**：`smoke` 里用一个非法前缀构造 `App`，断言 `points()->locate()` 仍然可用。

---

<a id="r3-02"></a>
### R3-02 · 🟡 低 · `pos_clock_offset_sec` 能被改坏，但改不回来

| 项 | 内容 |
|---|---|
| **位置** | `ConfigSchema.php`（登记为 `type: 'int'` + `readonly: true`）· `cp/routes.php` `/config/save` · `wwwroot/cp/cp.js` |

这一项是 NEW-02 新引入的自动维护字段，登记时标了 `readonly: true`，
标签也写着「自动维护，不用手填」。但那个标记目前是**装饰性的**：

* `/config/save` 只检查「键有没有登记」和 `validate()`，**不看 `readonly`**；
* `cp.js` 里 `readonly` 出现 **0 次** —— 后台把它渲染成普通的可编辑输入框；
* `type: 'int'` 的校验是 `ctype_digit()`，**拒绝负数** ——
  而 POS 比本机慢时这个值天然是负的（本环境实测 `-7200`）。

**实测**

```
  当前库里的偏差： -7200
  管理员改成 99999   → {"ok":true, …}          ← 写进去了
  改完库里的值：      99999
  管理员改成 -7200  → {"ok":false, "hint":"只能填非负整数"}   ← 改不回来
```

也就是**能改坏、改不回来**，只能等下一轮 Cron（≤20 分钟）自愈。

`posNow()` 里那道 `CLOCK_OFFSET_MAX_SEC = 86400` 的保险挡得住 99999，
但挡不住 86400 以内的值：

```
  管理员把偏差改成 3600（在保险以内，会真的生效）：
     posNow() = 2026-08-31 10:06:03   POS 真实 = 2026-08-31 07:06:03   偏差 +10800 秒 ❌

  跑一次 cron 自愈：
     [09:06:03] POS 时钟偏差更新为 -7200 秒
     posNow() = 2026-08-31 07:06:03   POS 真实 = 2026-08-31 07:06:03   偏差 +0 秒 ✓
```

在那 ≤20 分钟里，补记时限那道闸门的基准被挪走一小时。
影响有限（会自愈、且要管理员主动去动一个写着「不用手填」的字段），
但一个标了 `readonly` 却能写、且写不进合法值的字段，本身就是个坑。

**建议**：`/config/save` 认 `readonly`（直接拒绝）；类型换成能表达负数的
（新增一个 `int_signed`，或直接用 `text` + 自定义校验）。

---

<a id="r3-03"></a>
### R3-03 · 🟡 低（测试）· 一条依赖运行时刻的假绿断言

| 项 | 内容 |
|---|---|
| **位置** | `tests/smoke.php:2030`「★★ 几单结账时间跨度太大 → 告警」 |
| **性质** | **不是本轮回归** —— 上一版 `e1e4a58` 上同样失败。上一轮我在 21:30 跑，恰好是绿的 |

**现象**

本轮第一次跑 smoke 就红了一项。在上一版代码上重跑，**同样红** ——
所以不是这次改动带出来的，是一条一直存在的时间敏感断言。

**根因**

风控夹具里那张「捡小票形状」的单是 `now − 5 小时`（`tests/smoke.php:1887`），
而 `riskWatch()` 统计的窗口是**今天的营业日**（切点 02:00）。
只要跑测试的本地时刻落在 **02:00–07:00**，这张单就归到昨天的营业日、
被窗口排除，跨度从 5 小时缩成 5 分钟，`grant_span_wide` 告警不触发，断言就红。

**实测 —— 同一份代码、同一台机器，只改运行时区（也就是只改「现在几点」）**

```
  时区 UTC              本地时刻 06:56   「跨度太大→告警」✗ 失败
  时区 Europe/Madrid    本地时刻 08:56   「跨度太大→告警」✓ 通过
  时区 Asia/Shanghai    本地时刻 14:57   「跨度太大→告警」✓ 通过
```

> 补充：`smoke.php` 不 `require bootstrap.php`（它有自己的 autoloader），
> 所以它跑在 **PHP 默认时区**而不是配置里的 `Europe/Madrid`。
> 这让「几点算 02:00」还多了一层不确定。

**产品行为是对的** —— 结账在 02:00 之前的单本来就属于前一个营业日，
`riskWatch` 不把它算进今天是符合 `docs/01` §5.2 的。**错的是夹具**。

**这不是第一次**。同一段测试里，紧挨着的那条注释已经记下过同类问题：

> 这条测试因此会【随一天中的时刻时绿时红】：跑在 18:00–19:30 那个餐期空档里
> 就恰好是绿的，因为那时两张单都落在餐期之外、退回按天比。

当时给「餐期上限」那条加了第五张单来固定住，但**跨度告警这条没一起处理**。

**为什么值得修**：这条断言守的是防刷功能里唯一能管住内部人的那一层
（`docs/03` §12.4「能做的只有留下痕迹」）。CI 若排在夜里跑，它会稳定地红；
而它红的时候，人的第一反应是「又是那条不稳定的」——
下一次真出问题就没人看了。

**建议**：夹具改成「今天营业日窗口内、但跨度足够大」的两个时刻
（例如 `range(today)` 的起点 +1 小时 与 +6 小时），不要用 `now − N 小时`。

---

## 5. 建议的处理顺序

| 顺序 | 编号 | 理由 |
|---|---|---|
| 1 | R3-01 | 降级差最后一步；且 `repair` 现在会明确说错话，比不说更危险 |
| 2 | R3-03 | 一条会在夜里稳定变红的断言，会把整套 smoke 的可信度拖下水 |
| 3 | R3-02 | 会自愈，改动也小，可以跟着下一次动配置时一起处理 |

---

## 6. 本轮未覆盖的范围

* **`tests/e2e_pos.php`（95 项）未执行** —— 需可达的真实 POS 主库。
* **`tests/browser/` 有 5 个文件未执行**（`assignlock` / `bindamount` / `locate` /
  `merge` / `visitcount`）—— 需 `tests/sim/live_orders.json`。
  这 5 个覆盖的是找单/分摊/合并/计次这条链。
* **真实 POS 数据分布未复验** —— `docs/01` 中基于 88,616 行真实数据的结论
  本轮未复验，**仍以 `docs/01` 原文为准**。
* **`pos_clock_offset_sec` 的长期行为未验证** —— 本轮只验了「记录 → 读取 → 生效 → 自愈」
  这一圈。夏令时切换、POS 时钟被 NTP 纠正等场景下的表现没有测。
* **未做压力测试**，**未审查 `apk/` 容器侧**（不在本仓库内）。

---

*报告完 · 全部实测数据由 PHP 8.4.19 + MariaDB 10.11.14 + Chromium 环境产出。*
