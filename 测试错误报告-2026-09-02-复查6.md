# 测试错误报告（第六次复查 · 仍然只查「会不会让餐厅赔钱」）· 2026-09-02

> **本轮命题**（来自委托方）：**做了修复、还更新了文档，再测一次是不是有会造成餐厅经济损失的 bug。**
>
> **不改动仓库中任何既有文件**，涉及设计结论一律**引用** `docs/` 与源码原文。
>
> 前六轮：[`08-30`](./测试错误报告-2026-08-30.md)（16 条）·
> [`复查`](./测试错误报告-2026-08-31-复查.md)（+5）·
> [`复查2`](./测试错误报告-2026-08-31-复查2.md)（+3）·
> [`复查3`](./测试错误报告-2026-08-31-复查3.md)（+5）·
> [`复查4`](./测试错误报告-2026-08-31-复查4.md)（+3）·
> [`复查5`](./测试错误报告-2026-09-01-复查5.md)（+3）
>
> **被测版本**：`origin/main` @ `2a691fa`
> **本轮被复查的修复**：`5301e70`（复查5 三条 + 测试方法改造）+ `61d5d0a`（撤销场景测试 + `docs/13`）
> **提交人**：网络系统测试员
> **测试环境**：PHP 8.4.19 · MariaDB 10.11.14 · `php -S` 开 8 个 worker
> **POS 主库**：照 `pdb/pos_schema.sql` 重建的空表 + 手工夹具（**非真实门店数据**）

---

## 1. 一句话结论

**上一轮三条全部修好，实测逐条确认。这一轮只找到一条新的 —— 但它的位置很关键：整套防线守的是「谁在造券」，而这一条是「造出来的券没被记账」。**

| | 结论 |
|---|---|
| M-01 金额闸门不随份数放大 | ✅ **已修复**（`$amt < $min * $prt`） |
| M-02 补发护栏只认两个键 | ✅ **已修复**（两条路都告警，且护栏现在看得见等级） |
| M-03 日上限按日历零点切 | ✅ **已修复**（走营业日；撤销放大额度那条也修了） |
| **Z-01 用券吃的那一餐会不会又攒一次** | 🔴 **新发现** |

Z-01 一句话：**核销识别只靠一份「后台可改的自由文本」去猜 POS 折扣行的名字，
而 App 自己明明知道每一张券核销在哪一单，却从不回写、也从不对账。**
匹配串一旦对不上，用券吃的那一顿**照常攒一次** ——
免费餐自己在为下一顿免费餐攒进度，门槛 10 时**免费餐发放量约多 11%**，全程零告警。

---

## 2. 基线

| 套件 | 上轮 | 本轮 | 结果 |
|---|---|---|---|
| `tests/run.php` | 986 | **988** | 全部通过 |
| `tests/smoke.php --fresh` | 598 | **622** | 全部通过 |
| `tests/http_sweep.php` | 82 | **89** | 全部通过 |

与两个提交里写的数字完全对得上。

---

## 3. 上一轮三条：逐条复验，全部修好

### 3.1 M-01 金额闸门不随份数放大 —— ✅ 已修复

判据现在乘上了份数：

```php
if ($prt > 0 && $minPerVisitCents > 0 && $amt < $minPerVisitCents * $prt) {
```

用**完全相同**的探针（同一张 71.70 元三份套餐的单，`by_portion`，出厂门槛 5.00）再跑一次：

```
① 0.01 连着 1 份（旧玩法）
   结果：{"ok":false,"error":"amount_too_small_for_visit"}
② 5.00 连着三份全部（上一轮打通的那个）
   结果：{"ok":false,"error":"amount_too_small_for_visit"}   ← 现在也堵住了
③ 再来两趟同样操作
   B 最终：计次 0 · 累计消费 € 0.00 · 在手券 0 张
```

上一轮这里是「累计消费 € 20.00 换到一张券」。现在**一次都换不到**。

顺带说一句：这次配的性质穷举测试（`AllocationTest`：凡是通过校验并换到计次的，
每一次成本都 ≥ 门槛，穷举金额 × 份数的全部组合）比我这条探针更强 ——
它不需要有人先想到「5.00 换 3 份」这个玩法。

### 3.2 M-02 补发护栏只认两个键 —— ✅ 已修复（两条路都验了）

```
════ 第一条：换积分口径（visits → amount）════
  响应：{"key":"reward_mode","value":"amount","will_issue":37}
  告警：reward_backfill_risk | severity 3

════ 第二条：/tiers/save 把金卡门槛从「跟随全局 10」改成 2 次 ════
  响应：{"saved":true,"will_issue":12}
  告警：reward_backfill_risk | severity 3
```

上一轮这两条都是「界面无提示、告警一条没有」。
而且 `pendingAcrossMembers()` 现在**看得见等级**了 ——
同一位金卡会员，上一轮它算出 0（实际欠 12 张），现在算出 12。

护栏从「钉在哪几个键上」改成「钉在应发张数会不会变多」，方向是对的：
以后再加新维度自动被罩住。

### 3.3 M-03 日上限按日历零点切 —— ✅ 已修复（两支都验了）

把门店时区拨到本地 00:48（同一手法，代码只看挂钟与零点、02:00 的相对关系）：

```
  22:40 那笔 → 营业日 2026-09-02
  此刻 00:48 → 营业日 2026-09-02
  ★ 同一个营业日、同一个班次、同一个收银员

  零点过后再录 300.00：
    {"ok":false,"error":"exceeds_manual_daily_cap",
     "detail":{"cap":"300.00","used":"300.00","given":"300.00"}}
  这一个班次手工录入合计：€ 300.00（上限 € 300.00）
```

上一轮这里是「€ 600 对着 € 300 的上限」。

另外提交信息里自己找到的那一条比我报的更糟，也一并确认修好了：
撤销插入的负数流水 `source` 也是 `MANUAL`，原来统计没按 `entry_type` 过滤 →
已用额度会变成 **-300** → 额度不是还回来是**翻倍**。现在只算 `T_EARN`。

---

## 4. 顺手压了两条「修完之后新长出来的路」—— 都没问题

### 4.1 「录满 → 客人吃掉券 → 撤销 → 再录满」滚不起来 ✅

撤销会把手工录入的额度释放（那笔钱确实被撤掉了，不该再占额度）。
那么能不能靠这个反复滚？实测三轮：

```
── 第 1 轮 ──  录入 300.00 → 发券 6 张 → 客人全吃掉 → 撤销
── 第 2 轮 ──  录入 300.00 → 发券 0 张
── 第 3 轮 ──  录入 300.00 → 发券 0 张

══ 三轮下来 ══  白吃掉的免费餐：6 顿（不是 18 顿）
```

**滚不动。** 券被吃掉时 `rewards_issued` 不退（客人确实拿到了那份奖励），
所以第二轮重新录进同样的钱，`应发 − 已发` 仍然是 0。
这正是那条自愈公式该有的样子。

### 4.2 多桌合并之后整组撤销，券正确收回 ✅

```
① 合并三桌记账（by_order，门槛 3）→ 计次 3 · 已发券 1 · 在手券 1
② 整组撤销 → 计次 0 · 已发券 0 · 在手券 0 · 作废券 1
③ 不变量：应发 0 / 已发 0 → ✓ 成立
```

`reverseGroup` 逐条走 `reverseInTx`，收券没有多收也没有少收。

---

<a id="z-01"></a>
## 5. Z-01 🔴 用券吃的那一餐会不会又攒一次 —— 取决于一份「猜 POS 折扣行名字」的自由文本

### 5.1 现在这件事是怎么判的

一顿饭算不算「用券吃的」（`is_redeemed`），**唯一**的判据是拿
`redeem_line_patterns` 去匹配 POS 明细里那条负数折扣行的**名称**：

```php
// app/lib/PointsEngine.php:39
public const REDEEM_PATTERNS = ['TARJETA 10+1', '10+1'];
// db/seeds/001_sys_config.sql:145
(@store,'redeem_line_patterns','TARJETA 10+1,10+1',@now)
```

代码自己的注释也承认这份文本是会对不上的：

> 而 `PointsEngine::REDEEM_PATTERNS` 的注释承诺「名称会变……改后台 `sys_config` 即可，
> 无需改代码」—— **店家真改了 POS 里的名称：Pad 认得出，夜间校准认不出。**
> —— `ReconcileService.php`
>
> 名称是会变的（`Dto. -20%` 已换成 `-15%`）
> —— `SyncService.php`

### 5.2 而 App 自己知道答案，只是没拿出来用

`RewardService::redeem($couponId, $serialId, …)` 把 `serial_id` 存进了券：

```php
[self::ST_REDEEMED, $this->db->now(), $serialId, $operator['id'] ?? null, $couponId]
```

**这就是核销的地面真值 —— 哪一张券、核销在哪一单，一清二楚。**
但它只写在 `coupon` 行上：`redeem()` **不回写** `pos_order.is_redeemed`，
也没有任何地方拿这两份数据对过账。

### 5.3 实测

场景：店家把 POS 那条折扣行改名成「10+1 GRATIS」，忘了同步后台匹配串。
口径「来 10 趟送 1 次」·`once_per_period`·`free_meal_extra_earns` 关（全是出厂默认）。

```
① 第 10 次（3 天前）→ 计次 10，发券 1 张

② 昨天客人用这张券吃免费餐。POS 上折扣行叫「10+1 GRATIS」，匹配串认不出
   App 里核销：{"ok":true}
   核销后这张订单：{"serial_id":"RQ11","is_redeemed":0}   ← App 没有回写

③ 服务员照常拿卡记这一单
   记账：{"ok":true,"error":null}
   ★ 计次 11 ← 用券吃的那一餐【也攒了一次】

④ 对照组：匹配串对的时候（is_redeemed=1、净计次份数=0）
   记账：{"ok":false,"error":"redeemed"}   ← 正确拒掉，计次没有再加

⑤ 全店对账：App 自己知道答案，但没人拿它对
   coupon 已核销 1 张（每张都带 redeemed_serial_id）
   pos_order 里 is_redeemed=1 的：0 张
   告警：（一条都没有）
```

对照组证明**规则本身是对的**：认出来的时候拒得干干净净。
问题只在于「认不认得出」全押在一份自由文本上，而认不出时**没有任何人会知道**。

### 5.4 这一条要花多少钱

在「来 N 趟送 1 次」口径下，免费餐**自己也在为下一顿免费餐攒进度**：

| | 一顿免费餐要几顿付费餐换 |
|---|---|
| 正常（认得出核销） | 10 顿付费 → 1 顿免费，那一顿不计次 |
| 匹配串失配 | 9 顿付费 + 1 顿免费 = 10 次 → **9 顿付费就送 1 顿** |

稳态下免费餐发放量 **10 ÷ 9 ≈ +11.1%**。
门槛越低这个比例越大（门槛 5 时是 +25%）。

而且它**不是一次性事故，是持续渗漏** —— 每一位常客、每一个兑换周期都在漏，
账面上完全看不出异常：计次是真的、订单是真的、金额是真的。

### 5.5 现有的那道兜底只防了相反的方向

`SyncService::checkIntegrity()` 段落 C 加了一道比例兜底 —— 但方向是**反的**：

```php
// 超过 30% 说明匹配串几乎肯定框进了普通折扣
if ($rdPct >= 30) { … raiseOnce('redeem_rate_high', …) }
```

它防的是「**匹配串太宽**，把普通折扣也算成核销」——
那个方向的后果是**误伤客人**（计次和积分被没收），注释里写得很清楚。

**没有任何东西防相反的方向**：匹配串**太窄或失配**，一张都认不出来。
而让餐厅赔钱的恰恰是这一个方向。

这正好落在 `docs/13 §3.1`「修了那一处、没修那一类」那一格里 ——
同一个自由文本，两个方向的失效，只堵了不花钱的那一边。

### 5.6 修的方向（供参考，我不改代码）

**第一道（治本）**：`redeem()` 成功时，如果带了 `serial_id`，就把
`pos_order.is_redeemed` 一并写上 —— App 已经确知这一单是用券吃的，
不该再回头去猜 POS 的行名。这一道让匹配串从「唯一判据」降级成「补充判据」
（客人先吃后核销、或核销时没填单号的场景仍然靠它）。

**第二道（兜底）**：把 §5.5 那道比例检查补上**低位方向** ——
近 N 天 `coupon` 表里核销了 X 张，而 `pos_order.is_redeemed=1` 只有 Y 张，
`Y` 明显小于 `X` 就报警。两份数据都在本地库里，不用打 POS，
和现有的 `redeem_rate_high` 是同一个查询的另一半。

---

## 6. 顺带复核过、确认没问题的

| 检查项 | 结论 |
|---|---|
| 券作废之后会不会在后续消费中「复活」 | ✅ 不会（这一条他们自己也钉进了 smoke ㊺） |
| 已核销的券再核销 / 过期券 force 核销 | ✅ 状态检查排在 force 分支之前 |
| 手工录入额度靠「撤销」反复滚 | ✅ 滚不动（见 §4.1） |
| 多桌合并 + 整组撤销的收券 | ✅ 不多收不少收（见 §4.2） |
| 券过期（`expireStale`）会不会释放 `rewards_issued` | ✅ 不会 —— 过期券仍占额度，客人不会因为放过期而白得一张 |
| `is_free_meal` 开关能被普通收银员关掉 | ⚪ **不算缺陷**：这个标记本来就是**操作员声明**的（系统那一侧只出 `free_meal_suspect` 告警、不动标记），所以能标就能取消，是对称的。我的合成夹具没有 POS 明细行，跑不出那条兜底告警，所以这一格只作记录，不下结论 |

---

## 7. 一句话给老板

> 上一轮那三道差一寸的闸门都补齐了，而且这次是**连着测试方法一起改的** ——
> 性质穷举和「钱的总不变量」这两样，比再多几条断言都管用。
>
> 这一轮只剩一条：**造券那一侧现在守得很严，但「券花掉了」这件事
> 全靠猜 POS 上那行字叫什么名字**。猜不中的时候，
> 用券吃的那顿饭会当成正常一顿又攒一次 —— 每 9 顿付费就送一顿，而不是 10 顿。
> App 手里其实有确切答案，只是一直没拿出来对。

---

## 8. 附：本轮用到的复现脚本

都在会话临时目录下，**没有进仓库**：

| 脚本 | 用途 |
|---|---|
| `m1b_setup.php` / `m1b_probe.php` | 复验 M-01：71.70 三份套餐那张单 |
| `m2b_setup.php` | 复验 M-02：金卡会员 + `/config/save`、`/tiers/save` 两条路 |
| `m3b_probe.php` | 复验 M-03：跨零点的同一班次 + 撤销是否放大额度 |
| `loop_probe.php` | 手工录入额度能不能靠「吃掉券 → 撤销」滚起来 |
| `mg_probe.php` | 多桌合并 + 整组撤销的收券与不变量 |
| `rp2_probe.php` / `rp3_probe.php` | **Z-01**：匹配串失配时用券那一餐是否又计一次，以及对照组 |

---

*本报告只新增本文件，未改动仓库中任何既有文件。*
