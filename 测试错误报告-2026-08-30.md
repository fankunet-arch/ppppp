# 测试错误报告 · 2026-08-30

> **性质**：独立缺陷记录。本报告**不改动仓库中任何既有文件**，
> 涉及设计结论时一律**引用** `docs/` 下的原文（注明文件与小节），不复制、不改写、不并入其索引。
>
> **提交人**：网络系统测试员
> **被测系统**：餐厅 POS 旁路会员积分系统（`main` @ `b3fdbd5`）
> **测试范围**：全仓库代码审查 + 真实数据库 / 真实 HTTP 接口验证
> **测试环境**：PHP 8.4.19 · MariaDB 10.11.14 · Node 22
> **POS 主库**：照 `pdb/pos_schema.sql` 重建的空表 + 手工夹具（**非真实门店数据**）

---

## 1. 基线

进入缺陷排查之前，先把仓库自带的三套测试跑成基线。**全部通过**：

| 套件 | 前置条件 | 结果 |
|---|---|---|
| `tests/run.php` | 无 | 887 项通过 |
| `tests/smoke.php --fresh` | 真 MariaDB 专用空库 | 458 项通过 |
| `tests/http_sweep.php` | 真站点 + 真 POS 表结构 | 67 项通过 |

> `tests/e2e_pos.php`（95 项）与 `tests/browser/*.mjs`（525 项）本轮**未执行** ——
> 前者需要可达的真实 POS 主库，后者需要开发机图形环境，两者本环境都不具备。
> 因此这两条路径覆盖到的问题不在本报告的判断范围内。

**结论：基线全绿，说明现有断言覆盖的行为都是对的。本报告记录的是覆盖之外的部分。**

---

## 2. 缺陷汇总

| 编号 | 级别 | 摘要 | 位置 | 引用文档 |
|---|---|---|---|---|
| [BUG-01](#bug-01) | 🔴 严重 | 夜间 Cron 覆盖已记账订单的业务字段 → 多发积分、丢失计次 | `app/lib/Service/SyncService.php:158`<br>`app/lib/Repo/OrderRepo.php:36` | `docs/03` §4.2、`docs/04` §6.2.1 |
| [BUG-02](#bug-02) | 🔴 严重 | 达标发券无事务无锁 → 并发下多发免费餐券 | `app/lib/Service/RewardService.php:222` | `docs/03` §12.2 |
| [BUG-03](#bug-03) | 🟠 中 | `alloc_status` 读到被自身更新过的列 → 分配一半即标为「已全额」 | `app/lib/Repo/OrderRepo.php:113` | `db/migrations/001_init.sql:56` |
| [BUG-04](#bug-04) | 🟠 中 | `/order/locate` 的查找时间窗由客户端决定，无上限 | `app/api/routes.php:236` | `docs/02` 铁律 1、`docs/03` §1.2 |
| [BUG-05](#bug-05) | 🟠 中 | 经理手工录入无金额上限，且可自审自批 | `app/api/routes.php:918`<br>`app/cp/routes.php:196` | `docs/03` §10、§12.4 |
| [BUG-06](#bug-06) | 🟠 中 | 建号 PIN 闸门与服务层差两位 → HTTP 500，错误码指向 POS 护栏 | `app/cp/routes.php:471` | `docs/06` 错误代码表 |
| [BUG-07](#bug-07) | 🟠 中 | `card_prefix` 含 I/L/O/U 时整套实体卡功能失效 | `app/lib/CardNumber.php` | `docs/09`、`docs/10` |
| [BUG-08](#bug-08) | 🟠 中 | 值比对路径与记账路径用两套「可积分总额」算法 | `app/lib/Service/ReconcileService.php:148` | `docs/01` §2.11、`docs/03` §5 |
| [BUG-09](#bug-09) | 🟡 低 | 标记免费餐借用了券核销的审计动作名 | `app/api/routes.php:325` | `docs/03` §12.4 |
| [BUG-10](#bug-10) | 🟡 低 | 补记时限用应用服务器时钟对比 POS 时间 | `app/lib/Service/PointsService.php` | `docs/02` §2 |
| [BUG-11](#bug-11) | 🟡 低 | 夏令时前跳日的营业日窗口重叠一小时 | `app/lib/BusinessDay.php` | `docs/01` §5.2 |
| [BUG-12](#bug-12) | 🟡 低 | `splitEvenly()` 会造出「0 元 N 份」，与代码注释的断言矛盾 | `app/lib/PointsEngine.php` | `docs/03` §3.2、§13 |
| [BUG-13](#bug-13) | 🟡 低 | `pointsBaseCents()` 的税额缩放是恒等运算（死代码） | `app/lib/PointsEngine.php` | `docs/01` §2.11 |
| [BUG-14](#bug-14) | 🟡 低 | `/dashboard` 的「今天」混用营业日与自然日两个口径 | `app/cp/routes.php` | `docs/01` §5.2 |
| [BUG-15](#bug-15) | 🟡 低 | 未登记的配置键不校验；`drink_major_group` 后台不可见 | `app/lib/ConfigSchema.php` | `docs/04` §6.1 |
| [BUG-16](#bug-16) | 🟡 低 | README 标称的测试项数与实际不符，5 项静默跳过 | `README.md` · `tests/run.php` | — |

**级别定义**

* 🔴 **严重** —— 会导致积分/次数算错，或造成直接经济损失，且在正常操作路径上可触发。
* 🟠 **中** —— 绕过后台配置、越权、接口 500，或某项功能在特定配置下整体失效。
* 🟡 **低** —— 审计留痕、口径一致性、边界条件、文档偏差。不影响金额正确性。

> **一条共性**：BUG-01 / 02 / 03 / 07 都**位于现有测试已经执行到的代码路径上**，
> 只是没有断言检查它做对了没有。详见每条的「现有测试为何未拦截」。

---

## 3. 缺陷详情

<a id="bug-01"></a>
### BUG-01 · 🔴 严重 · 夜间 Cron 覆盖已记账订单的业务字段

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/Service/SyncService.php:158` `storeOrder()`<br>`app/lib/Repo/OrderRepo.php:36` `upsert()` 的 `ON DUPLICATE KEY UPDATE` |
| **触发条件** | 营业时段的每一张订单，在结账后一轮 Cron（≤20 分钟）内必然发生 |
| **引用文档** | `docs/03` §4.2「撤销后改记法」是本条的放大路径；`docs/04` §6.2.1 BOX/COMBO `earns_points=0` |

**根因**

`storeOrder()` 的意图在其注释中写得很明确，且是正确的：

> 补抓时只落订单头与金额，把 total/portions 留到 Pad 端 locate() 时再算。

但它调用的 `upsert()` 是一条 `INSERT … ON DUPLICATE KEY UPDATE`，而那份 UPDATE 列表包含：

```
total_amount, excluded_amount, tax_amount,
portions_counted, portions_uncounted,
is_redeemed, redeem_amount
```

于是「新单的占位值」在**已存在的行上变成了覆盖值**。`storeOrder()` 传的是
`excluded=0` / `portions=0`，且完全不传 `is_redeemed` / `redeem_cents` / `tax_cents`，
全部落进已有的行里。

`allocated_amount` 与 `allocated_portions` 不在 UPDATE 列表中，**不会被覆盖** ——
镜像因此进入「总额是毛额、已分配是净额、份数为 0」的自相矛盾状态。

**为什么每一单都会中招**

`bin/crontab.example`：

```
*/20 11-23,0-1 * * *   php bin/cron.php incremental
```

水位线每轮推进到「上一轮的 posNow」，21:05 结的账必然落在 21:20 那一轮的窗口内。

**复现步骤**

1. 夹具：一张 90.00 的堂食单 = 3 份套餐 71.70（计次、计分）+ 1 份 `BOX 1` 18.30
   （`meal_item_rule` 中 `earns_points=0`，按 `docs/04` §6.2.1 不计金额积分）
2. `POST /api.php/order/locate` `{"table_name":"77"}`
3. `POST /api.php/points/grant` 整单记给会员 A
4. `php bin/cron.php incremental`
5. 查 `pos_order`

**实测输出**

```
① Pad 按桌号找到这一单（读明细、算排除项）
   POS 小票金额 90.00 ；可积分总额 total = 71.70  排除 = 18.30  计次份数 = 3

② 整单记给会员 A
   记入积分 71 分 / 计次 1
         total_amount: 71.70      excluded_amount: 18.30
     portions_counted: 3          allocated_amount: 71.70
   allocated_portions: 3

③ php bin/cron.php incremental
   OK 增量补抓  {"ok":true,"batches":1,"rows":2,"windows":1}
         total_amount: 90.00   ← 排除项没了
      excluded_amount: 0.00    ← 归零
     portions_counted: 0       ← 归零
     allocated_amount: 71.70   ← 没变，于是与 total 对不上
   allocated_portions: 3
```

核销单（明细含 `TARJETA 10+1` 折扣行）同样中招：

```
   is_redeemed:      1  →  0
   redeem_amount: 23.90 → 0.00
```

**影响 —— 一个正常操作即放大为多发积分**

`docs/03` §4.2 把「服务员先把整单记给一人，客人要求改记 → 撤销后重记」
列为典型场景。接着上面的状态继续走这条路：

```
④ 撤销那一笔  → {"ok":true,"reversal_id":3}
     total_amount: 90.00   allocated_amount: 0.00
     portions_counted: 0   allocated_portions: 0

⑤ 重新记账（服务端此刻认为这一单可积分 90.00）
     {"ok":true, entries:[{"amount":"90.00","points":90,"visits":0}]}

   小票 90.00，其中 BOX 18.30 按规则【不计金额积分】
   正确应给 71 分 / 计次 1 —— 实际给了 90 分 / 计次 0
```

`point_ledger` 最终留下：

| id | amount | points | counted_visit |
|---|---|---|---|
| 2 | 71.70 | 71 | 1 |
| 3 | −71.70 | −71 | −1 |
| 4 | **90.00** | **90** | **0** |

**两个方向同时错**：积分多给 19 分（BOX 的 18.30 本不该计分），
计次少给 1 次（十送一进度凭空少一格，客人无从察觉）。
`points_include_tax=0` 时税额也会被算回去，偏差更大。

附带损失：

* `/report/daily`、`/dashboard` 的金额全部失真 —— `SUM(total_amount)` 是毛额、
  `allocated_amount` 是净额，经理看到的每一天都像「有一大截积分没发出去」。
* `is_redeemed` / `redeem_amount` 这条审计线索被抹掉，事后无法回答
  「这一单当初是不是用券免的」。

**现有测试为何未拦截**

`tests/smoke.php:508` 确实在发分之后调用了 `incremental()`，但断言只有两条：
「执行成功」与「重复执行不产生重复订单（行数不变）」。
**没有任何断言检查那一行的内容是否被改坏**；而夹具订单 A 恰好没有排除项，
`total_amount` 前后都是 71.70，差别不显现。

**建议**

`upsert()` 拆成两个用途明确的方法，或增加 `$fromSync` 开关：
补抓路径只写订单头与 `should/actual/original`，
`total_amount / excluded_amount / tax_amount / portions_* / is_redeemed / redeem_amount`
一律走 `COALESCE`（已有值则不动），仅 `buildContext()` 有权写它们。

**应补断言**：`smoke` 在 `incremental()` 之后，断言已记账订单的
`total_amount / excluded_amount / portions_counted / is_redeemed` 与之前逐一相等。

---

<a id="bug-02"></a>
### BUG-02 · 🔴 严重 · 达标发券在并发下多发免费餐券

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/Service/RewardService.php:222` `checkAndGrant()` |
| **触发条件** | 同一会员的两笔记账在时间上重叠；或 `issue()` 完成后、`UPDATE` 之前进程终止 |
| **引用文档** | `docs/03` §12.2 —— 多桌合并的排序防死锁设计说明本系统以多 Pad 并发为前提 |

**根因**

方法注释称「幂等靠 `rewards_issued` —— 重复调用不会多发」。该结论仅在**串行**下成立。
整个方法没有事务、没有 `FOR UPDATE`：

```php
$m = $this->members->findById($memberId);            // 读 rewards_issued
$p = $this->progressOf($m, $tier);                   // earned - issued = pending
for ($i = 0; $i < $p['pending']; $i++) { $out[] = $this->issue(...); }   // 先发券
$this->db->exec('UPDATE member SET rewards_issued = rewards_issued + ? …'); // 后记账
```

两个请求同时进到「读」这一步，各自算出 `pending = 1`，各发一张。

**复现步骤**

1. 建一位 `visit_count=10`、`rewards_issued=0` 的会员，规则「每 10 次送 1 次」（应当只发 1 张）
2. 4 个 PHP 进程预热连接后对齐到同一时刻调用 `checkAndGrant()`
3. 查 `coupon` 与 `member.rewards_issued`

**实测输出**

```
3357 granted=1 pending=0
3358 granted=1 pending=0
3359 granted=1 pending=0
3360 granted=1 pending=0
--- 结果 ---
rewards_issued   4
coupons_issued   4      ← 4 张免费餐券
```

**影响**

四张券 = 四顿免费的饭。券可正常核销；`void()` 不回退 `rewards_issued`，
事后只能人工逐张作废。

**现实中的触发路径**

* 门店有多台 Pad —— `grantMerged()` 专门为「两台 Pad 同时合并」写了排序防死锁
  （`docs/03` §12.2），说明多 Pad 并发是本系统的**既定前提**；
* 同一位客人的两桌单被两名收银员几乎同时记账；
* **不需要并发的一条**：`issue()` 全部完成后才 `UPDATE rewards_issued`，
  中间任何一次进程终止（PHP 超时、平板断网重试打断 FPM），
  券已发出而计数未加 —— 下一次记账会把同样的券再发一遍。

**现有测试为何未拦截**

`smoke` 只在单进程内顺序调用 `checkAndGrant()`，串行路径本身是对的。

**建议**

整段包进 `db->transaction()`，第一步 `members->lockById()` 取行锁；
`issue()` 与 `UPDATE … rewards_issued` 必须在同一事务内。

**应补断言**：多进程并发调用后，`COUNT(coupon) == floor(progress / threshold)`。

---

<a id="bug-03"></a>
### BUG-03 · 🟠 中 · `alloc_status` 读到被自身更新过的列

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/Repo/OrderRepo.php:113` `applyAllocation()` |
| **触发条件** | 任何一次「本次分配额 ≥ 剩余额的一半」的部分分配 |
| **引用文档** | `db/migrations/001_init.sql:56` —— `0=未分配 1=部分分配 2=已全额分配` |

**根因**

```sql
SET allocated_amount = allocated_amount + ?,
    alloc_status = CASE
        WHEN allocated_amount + ? >= total_amount AND total_amount > 0 THEN 2
```

MySQL / MariaDB 的 `UPDATE` 赋值**从左到右求值，后面的表达式看到的是前面已更新的值**
（与标准 SQL 不同）。CASE 里的 `allocated_amount` 已是新值，
条件实际为 `旧值 + 2×本次 >= 总额`。

**实测输出**（MariaDB 10.11）

```
初始                      total=100.00 allocated=0.00   alloc_status=0
分配 25.00 / 共 100.00    total=100.00 allocated=25.00  alloc_status=1
再分 25.00（累计 50.00）  total=100.00 allocated=50.00  alloc_status=1
再分 25.00（累计 75.00）  total=100.00 allocated=75.00  alloc_status=2   ← 还剩 25.00
再分 25.00（累计 100.00） total=100.00 allocated=100.00 alloc_status=2
```

四人 AA 的第二人一提交，整单即被标为「已全额分配」。

**影响**

**当前有限**：全仓库只有 `/report/daily` 读取该列，且只判 `> 0`，不区分 1 与 2。
但任何后续代码若按 `alloc_status = 2` 判定「这单已记完」都会立即出错。

**现有测试为何未拦截**

`tests/smoke.php:419` 与 `:455` 只覆盖「全额分配」与「撤销回 0」两端，
**中间的部分分配没有任何断言**。

**建议**

CASE 内直接写 `allocated_amount >= total_amount`（此时它已是新值），去掉多余的 `+ ?`。

**应补断言**：部分分配后 `alloc_status == 1`。

---

<a id="bug-04"></a>
### BUG-04 · 🟠 中 · `/order/locate` 的查找时间窗由客户端决定

| 项 | 内容 |
|---|---|
| **位置** | `app/api/routes.php:236` |
| **触发条件** | 任何已登录的收银员，构造一次带 `window_minutes` 的请求 |
| **引用文档** | `docs/02` 铁律 1「绝不全表扫」；`docs/03` §1.2 30 分钟窗口的由来；`docs/06` 后台配置 `order_lookup_window_min` |

**根因**

```php
$win = Api::int($b, 'window_minutes', 0);
$r   = $app->points()->locate($table, $win > 0 ? $win : null);   // 无上限
```

该值一路传到 `PosReader::findRecentByTable()` 的
`WHERE order_end_time >= NOW() - INTERVAL ? MINUTE`，中间无任何封顶。

**实测输出**

```
后台配置 order_lookup_window_min = 30

① 不带 window_minutes（走后台配置）
   window= 30       候选单数= 1

② 客户端自己传 window_minutes = 5256000（十年）
   window= 5256000  候选单数= 2
   → ['2026-08-30 19:24:36', '2026-07-31 19:49:36']
```

**影响**

1. **后台配置形同虚设。** `order_lookup_window_min` 与 `lookup_fallback_window_min`
   是店家用来限制「能往回捞多久的单」的。补记时限闸门（`late_grant_minutes`）
   只在**记账**时拦、**找单不拦**，而找单本身就返回别桌的金额、份数、
   菜品明细、已记给谁。
2. **对 POS 的无上限扫描入口。** `docs/02` 的铁律第一条是「绝不全表扫」；
   十年窗口 + `table_name` 无索引 = 沿 `idx_order_end_time` 倒扫到底。
   `docs/README.md` 的「核心前提」第 2 条写明「POS 主机性能极度受限」，
   而这个接口任何已登录账号都能打。

**对照**：`/order/locate-invoice` 把「会被人拿去当探针」写进了注释，
做了四层防护（回话不区分、日期谁都不给、服务端砍掉、留痕告警）。
按桌号这条路是同一类入口，**一层都没有**。

**建议**

`$win = min($win, cfg('lookup_fallback_window_min', 60))` ——
客户端最多只能放宽到后台设定的「放宽窗口」。

**应补断言**：`http_sweep` 中带超大 `window_minutes` 时，响应的 `window` 被服务端夹到配置上限。

---

<a id="bug-05"></a>
### BUG-05 · 🟠 中 · 经理手工录入无金额上限，且可自审自批

| 项 | 内容 |
|---|---|
| **位置** | `app/api/routes.php:918` `/points/manual`<br>`app/cp/routes.php:196` `/reviews/decide` |
| **触发条件** | 任一经理及以上账号 |
| **引用文档** | `docs/03` §10 降级路径；§12.4「对内部作案，事前拦截在结构上无效 —— 能做的只有留痕」 |

**根因**

```php
// 路由：经理身份即视为已审批
'approved_by' => $op['is_manager'] ? $op['id'] : null,

// manualGrant()：有 approved_by 就不查上限
if ($amountCents > $limit && empty($operator['approved_by'])) { … }
```

`manual_entry_limit`（出厂 200.00）对经理**完全不生效，且无任何替代上限**。

**实测输出**

```
后台配置 manual_entry_limit = 200.00
经理提交 999999.99 欧手工录入 →
   {"ok":true,"data":{"ledger_id":1,"points":999999,"review_required":true}}
   member: points_balance=999999  total_spent=999999.99

待复核 id=1 金额=999999.99 分=999999 提交人=系统管理员
同一个管理员点「通过」→ {"ok":true,"accepted":true}
   point_ledger: operator_id=1  approved_by=1  review_status=2
```

**说明**

`docs/03` §12.4 的取舍（内部作案只能留痕、不能事前拦截）是成立的，
**本条不主张增加拦截**。但仍有两点应修：

1. **缺少绝对上限。**「经理可以破例」与「经理可以一次记 100 万欧」是两件事。
2. **自审自批无留痕差异。** `review_accept` 审计条目不记录「提交人 == 审批人」，
   而 `docs/03` §12.4 的整套设计恰恰依赖「痕迹每周有人看一眼」。

**建议**

* 增加独立的 `manual_entry_hard_limit`，超过它任何角色都不放行；
* `approved_by === operator_id` 时审计写成 `review_accept_self` ——
  与既有的 `point_grant_forced`、`card_replace_forced` 同一套做法，后台筛一下即可看到全部自审记录。

---

<a id="bug-06"></a>
### BUG-06 · 🟠 中 · 建号 PIN 闸门与服务层差两位 → HTTP 500

| 项 | 内容 |
|---|---|
| **位置** | `app/cp/routes.php:471` `/operators/create` |
| **触发条件** | 管理员建号时填 4 或 5 位 PIN |
| **引用文档** | `docs/06` 错误代码表 —— E203 = 「PosDb 护栏：非 SELECT / 带分号 / 没有 LIMIT」 |

**根因**

```php
// 路由
if (… || strlen($pin) < 4) { Api::fail('bad_request', 400, ['hint' => 'PIN 至少 4 位']); }
try { $id = $app->auth()->createOperator(…); }
catch (\PDOException $e) { … }          // 只接 PDOException

// 服务层
public const MIN_PIN = 6;
if (strlen($pin) < self::MIN_PIN) { throw new \InvalidArgumentException(…); }
```

**实测输出**

```
PIN=1234   → {"ok":false,"error":"server_error",
              "message":"系统内部错误，请稍后重试（错误代码 E203-57C89F）"}
PIN=123456 → {"ok":true,"data":{"id":5}}
```

**影响 —— 三处都错**

* **提示语误导**：写着「PIN 至少 4 位」，真实下限是 6。
* **状态码错**：参数问题回 500 而非 400。
* **错误码指错方向**：`InvalidArgumentException` 继承 `LogicException`，
  `Api::classify()` 归为 **E203**；按 `docs/06` 的错误代码表，
  E203 指向 POS 主库护栏。管理员会去查 POS，而问题在建号表单里。

**建议**

路由阈值改用 `AuthService::MIN_PIN`（不再写字面量 4），提示语跟随常量；
`catch` 补上 `\InvalidArgumentException`；
`Api::classify()` 将其从 PosDb 护栏的错误码中分离。

**应补断言**：`http_sweep` 中 4 位 PIN 建号返回 400 且 `error === 'bad_request'`。

---

<a id="bug-07"></a>
### BUG-07 · 🟠 中 · `card_prefix` 含 I/L/O/U 时整套实体卡功能失效

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/CardNumber.php` —— `normalize()` 与构造函数 |
| **触发条件** | `app/config/config.php` 的 `card_prefix` 含 I、L、O 或 U |
| **引用文档** | `docs/09` 卡号格式与防伪；`docs/10` 上线步骤（发卡是最后一步） |

**根因**

`normalize()` 将印刷体下易读错的字符映射回去：

```php
return strtr($s, ['O' => '0', 'I' => '1', 'L' => '1', 'U' => 'V']);
```

该映射**作用于整个输入串，包括前缀**；而 `$this->prefix` 保存的是配置原值，
**未经同一映射**。于是 `isWellFormed()` 中的
`str_starts_with($n, $this->prefix)` 恒为假。

**实测输出**

```
prefix=TK    make()=TK00000123Q7X    normalize()=TK00000123Q7X    isWellFormed=OK
prefix=VIP   make()=VIP00000123Q7X   normalize()=V1P00000123Q7X   ❌ 自己生成的卡号被判为非法
prefix=GOLD  make()=GOLD00000123Q7X  normalize()=G01D00000123Q7X  ❌
prefix=CLUB  make()=CLUB00000123Q7X  normalize()=C1VB00000123Q7X  ❌
prefix=OK    make()=OK00000123Q7X    normalize()=0K00000123Q7X    ❌
```

**影响**

`card_prefix` 是 `config.php` 中自由填写的值，注释只写「1~4 位字母」，
构造函数也只校验 `^[A-Z]{1,4}$`。**`VIP` 是本系统最可能被填的前缀**
（前端桥接文件即名为 `sushivip-bridge.js`）。填了之后：

* `/card/lookup`、`/card/status`、`/member/create` 全部返回 `card_malformed`
  （界面提示「卡号不完整，请重新扫描或核对卡面号码」）；
* `CardRepo::findByCardNo()` 拿归一化后的串去查，而 `generateBatch()`
  存的是未归一化的串 —— **永远查不到**；
* 按 `docs/10` 的上线步骤，发卡是最后一步 —— **卡已经印出来了才会发现**。

**现有测试为何未拦截**

`tests/cases/CardNumberTest.php:14` 只测 `new CardNumber('TK')`，恰好是安全字符。

**建议**

构造函数对前缀做同样的 `normalize()`；或直接拒绝含 I/L/O/U 的前缀并说明原因
（后者更好 —— 让 `docs/10` 的操作者在填配置时就知道）。

**应补断言**：`CardNumberTest` 参数化覆盖 `VIP` / `GOLD` / `CLUB` / `OK` 四个前缀。

---

<a id="bug-08"></a>
### BUG-08 · 🟠 中 · 值比对与记账用两套「可积分总额」算法

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/Service/ReconcileService.php:148` `verifyOne()` |
| **触发条件** | 订单 `should/actual` 变更后走值比对（`docs/01` §3.4 实测约 2.9% 的订单被结账后修改） |
| **引用文档** | `docs/01` §2.11 税额口径；`docs/03` §5 十送一核销识别 |

**根因**

```php
// 值比对路径
$analysis = PE::analyzeDetail($detail, $this->rules);          // ← 少了 redeemPatterns
$newTotal = PE::pointsBaseCents($nowShould, $nowActual,
                $nowOriginal, $analysis['excluded_cents']);    // ← 少了 $taxCents

// 记账路径（PointsService::buildContext）
$analysis = PE::analyzeDetail($detail, $this->rules, $this->redeemPatterns());
$taxCents = cfg('points_include_tax') === '1' ? 0 : (int)$o['tax_cents'];
$baseCents = PE::pointsBaseCents(…, $excluded, $taxCents);
```

**影响**

1. **`redeem_line_patterns` 未传** —— 退回硬编码的 `['TARJETA 10+1','10+1']`。
   `PointsEngine::REDEEM_PATTERNS` 的注释承诺「名称**会变**……改后台 `sys_config`
   即可，无需改代码」；该承诺在校准路径上不成立：店家改了 POS 里的名称，
   Pad 认得出，夜间校准认不出。
2. **`points_include_tax` 未生效** —— 该配置为 `0` 时，`total_amount` 落库是
   **不含税**的，而重算出的 `newTotal` 是**含税**的。两数天然不等，
   `newTotal >= $oldTotal` 恒成立 → 走「金额变大，不自动补分」分支，
   推一条 `amount_changed` 告警。

**不至于错发积分**（只在金额确有变动的订单上走到这段），
但会让本该自动冲正的单被挂成人工待办，同时污染告警队列。

**建议**

把「算一单的 total」抽成两边共用的方法，从结构上防止再次分叉。

---

<a id="bug-09"></a>
### BUG-09 · 🟡 低 · 标记免费餐借用了券核销的审计动作名

| 项 | 内容 |
|---|---|
| **位置** | `app/api/routes.php:325` `/order/free-meal` |
| **引用文档** | `docs/03` §12.4 —— 留痕是唯一手段，故动作名必须可筛 |

```php
$app->orders()->markFreeMeal($serial, $isFree);
$app->audit()->log('coupon_redeem', [ … 'detail' => ['is_free_meal' => $isFree] ]);
```

**实测**（`audit_log`）：

```
action          target_type  target_id    detail
coupon_redeem   order        2608300001   {"is_free_meal":true}
```

`RewardService::redeem()` 使用同一个 `coupon_redeem`。CP 审计页按 `action` 筛，
于是「真的核销了一张券」与「服务员点了下免费餐开关」混在同一个桶里。
这与 `PointsService::auditForced()` 注释中确立的原则冲突
（「单独一个 action 名，筛一下就是全部破例」）。

**建议**：改为 `order_free_meal`。

---

<a id="bug-10"></a>
### BUG-10 · 🟡 低 · 补记时限用应用服务器时钟对比 POS 时间

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/Service/PointsService.php` `checkGates()` |
| **引用文档** | `docs/02` §2 与 `PosReader::now()` 注释：「时间基准必须取自主库（时钟/夏令时偏差）」 |

```php
$age = (time() - strtotime($o['order_end_time'])) / 60;
```

`order_end_time` 来自 POS 主库，`time()` 是应用服务器。
`locateByInvoice()` 的回溯天数判断确实用了 `$this->pos->now()` ——
**只有这一处用了本地时间**。

POS 是一台老 Windows 机器，时钟漂移常见：

* POS 快 2 小时 → `$age` 为负 → 补记时限**永不触发**；
* POS 慢 2 小时 → **每一单**都超时限，每次记账都要叫经理。

两个方向都难排查，界面只会显示「这一单超出普通记账范围」。

**建议**：与 `locateByInvoice()` 一致，改用 `$this->pos->now()`；POS 不可达时再退回 `time()`。

---

<a id="bug-11"></a>
### BUG-11 · 🟡 低 · 夏令时前跳日的营业日窗口重叠一小时

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/BusinessDay.php` `range()` |
| **引用文档** | `docs/01` §5.2 营业日切点 02:00 |

`range()` 用 `new DateTimeImmutable($date . ' 02:00:00')`，
而 Europe/Madrid 在春季前跳那天 **02:00 这一时刻不存在**，PHP 归一化为 03:00：

```
2026-03-28 -> [2026-03-28 02:00:00, 2026-03-29 03:00:00)
2026-03-29 -> [2026-03-29 03:00:00, 2026-03-30 03:00:00)
2026-03-30 -> [2026-03-30 02:00:00, 2026-03-31 02:00:00)
                             ↑ 与上一行重叠 1 小时
```

`countedThisSitting()` / `countGrantsInSitting()` 后面还有 `sameSitting()` 兜底，
不会误判；但 `riskWatch()` 直接使用 `earnedInRange()` 的结果统计「今天记了几次」，
一年中有一天会多计入一小时的流水。影响极小，但结果确实是错的。

---

<a id="bug-12"></a>
### BUG-12 · 🟡 低 · `splitEvenly()` 会造出「0 元 N 份」，与注释断言矛盾

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/PointsEngine.php` —— `validateAllocations()` 的注释 vs `splitEvenly()` 的行为 |
| **引用文档** | `docs/03` §3.2 AA 分摊；§13 计次口径 |

`validateAllocations()` 注释称：

> splitEvenly 不会造出这种分配（余数给第一位，基数份额是同增同减的），所以 AA 正常拆分不受影响。

**实测输出**

```
金额2分 份数3 人数5 -> 2/1  0/1  0/1  0/0  0/0   ❌ 出现了 0 元却带份数的分配
金额3分 份数3 人数5 -> 3/1  0/1  0/1  0/0  0/0   ❌
金额0分 份数3 人数3 -> 0/1  0/1  0/1              ❌

/points/split 返回的这一组，交回 /points/grant 会被自己拒掉：
{"ok":false,"error":"portions_without_amount","sum_amount":0,"sum_portions":0}
```

原因：金额余数给第一位、份数余数逐个摊开，两者的分配方式**不一样**
（这是有意的，见 `docs/03` §13 与该方法注释），
所以当 `intdiv(金额, 人数) == 0` 时，第 2…rem 位就会拿到「0 元 + 1 份」。

现实中要求「剩余金额的分数 < 人数」（如剩 0.03 € 分给 5 人），极难发生，
**不构成金额风险**（守恒的两层都在）。但那句注释是错的，后来者多半会信它。

**建议**：修正注释，或让 `splitEvenly()` 在 `amount == 0` 时不发份数。

---

<a id="bug-13"></a>
### BUG-13 · 🟡 低 · `pointsBaseCents()` 的税额缩放是恒等运算

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/PointsEngine.php` `pointsBaseCents()` |
| **引用文档** | `docs/01` §2.2 `LEAST(should, actual)`；§2.11 税额与小票 SubTotal 吻合 |

```php
$base = min($shouldCents, $actualCents);
if ($taxCents > 0 && $base > 0) {
    $base = max(0, $base - Money::scale($taxCents, $base, max(1, min($shouldCents, $actualCents))));
}
```

此刻 `$base` 恰好等于 `min($shouldCents, $actualCents)`，
故 `scale($tax, $base, $base) === $tax` —— 这个 `scale()` **永远是恒等的**，
等价于 `$base -= $taxCents`。它读起来像「按比例折算税额」，但并不是。

**实测输出**

```
should=58.25 actual=60.00 tax=5.30 -> base=52.95   （= 58.25 - 5.30）
should=58.25 actual=30.00 tax=5.30 -> base=24.70   （整单的税，从半单里扣）
```

第二行是实际的口径问题：`actual < should` 时（`docs/01` §2.2 实测全表仅 1 行），
基数取较小的 `actual`，税额却是整单的。影响可忽略。

**建议**：要么写成真正的比例折算，要么直接写 `$base -= $taxCents` 并说明为何可以。

---

<a id="bug-14"></a>
### BUG-14 · 🟡 低 · `/dashboard` 的「今天」混用两个口径

| 项 | 内容 |
|---|---|
| **位置** | `app/cp/routes.php` `/dashboard` |
| **引用文档** | `docs/01` §5.2 营业日切点 02:00；`db/seeds/002_meal_period.sql` 晚市 19:30–02:00 |

```php
$today = $app->businessDay()->of(date('Y-m-d H:i:s'));       // 营业日，02:00 切点
'orders_today'  => … WHERE business_date = $today
'granted_today' => … WHERE created_at >= date('Y-m-d 00:00:00')   // 自然日
'points_today'  => … WHERE created_at >= date('Y-m-d 00:00:00')   // 自然日
```

00:00–02:00 之间（晚市餐期尚未结束），`orders_today` 算的是昨天的营业日，
`granted_today` / `points_today` 已跳到今天。夜班收银员看到的首页数字对不上。

另外 `points_today` 不筛 `entry_type`，把撤销与冲正的负分也算了进去 ——
这多半是有意的（净额），但与 `granted_today` 只数 `entry_type=1` 的口径不一致，
建议把标签写清楚。

---

<a id="bug-15"></a>
### BUG-15 · 🟡 低 · 未登记的配置键不校验；`drink_major_group` 后台不可见

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/ConfigSchema.php` `validate()`；`app/cp/routes.php` `/config/save` |
| **引用文档** | `docs/04` §6.1 `sys_config` 默认值；`ConfigSchema` 自身注释「加新配置项时必须在这里登记」 |

```php
$meta = self::ITEMS[$key] ?? null;
if ($meta === null) { return null; }   // 未登记项不校验
```

`/config/save` 只判 `$key !== ''`，管理员可向 `sys_config` 写入任意键值。
不算越权（本就是管理员），但会让该表逐渐积累无人认识的行。

**实测**（交叉核对「代码读取的键」与「Schema 登记的键」）：

```
用到但没登记：drink_major_group      （app/lib/Service/MaintenanceService.php:57）
登记了但没用：（无）
```

`drink_major_group` 决定巡检时排除哪个 `major_group`（酒水）。
按 `MaintenanceService::auditMenuRules()` 的注释，填错会让 32 瓶红酒每天推告警，
或反过来把整个套餐组排除掉 —— **而后台看不到、也改不了这一项**。

**建议**：登记进 `ConfigSchema::ITEMS`；并考虑让 `validate()` 对未登记键直接拒绝。

---

<a id="bug-16"></a>
### BUG-16 · 🟡 低 · README 标称的测试项数与实际不符

| 项 | 内容 |
|---|---|
| **位置** | `README.md`「测试」小节；`tests/run.php` |

**实测输出**

```
无 app/config/config.php ：887 项断言
有 app/config/config.php ：892 项断言
README 写的            ：892 项
```

差的 5 项只在配置文件存在时才执行（`BootGuardTest` / `ContainerCompatTest` 一类）。
新人 clone 后跑 `php tests/run.php` 会看到 887，与 README 对不上 ——
而那 5 项恰恰是检查部署配置的。**静默跳过比跑不过更危险。**

**建议**：无 `config.php` 时明确打印「跳过 5 项（缺 app/config/config.php）」。

---

## 4. 确认无问题的项

以下均为专门构造场景验证过、确实拦住了的行为，一并记录以界定本报告的边界：

| 行为 | 验证方式 |
|---|---|
| 金额守恒 `allocated ≤ total` | 反复分、跨提交分、撤销后再分，均未突破 |
| `member_already_on_order` | 同一张卡在同一张单上记第二次被挡（跨提交重复） |
| `portions_without_amount` | 「0 元要 1 份」被硬拒；一笔里混进 0 元的人整笔回滚 |
| `too_many_members` | 3 份的单要记 5 位被拒；记满 3 位后第 4 位仍拒 |
| 小票号探针防护（`docs/03` §12） | 收银员查「不存在的号」与「超时效的真单」回话完全一致（`unavailable`）；响应体中搜不到任何日期；经理拿得到真实原因但同样拿不到日期 |
| 越权 | 收银员账号登不进 CP；收银员强制核销被拒 |
| 纸质券不误判（`docs/03` §5.5） | `CUPON DE 5 EUROS` 未被当作十送一核销 |
| 多语言（`docs/12`） | `X-Lang: es` 时错误文案为西语；无法识别的语言码不会打崩接口；中西两张文案表键完全对齐 |
| 引导容错（`docs/07`） | 本地库停止时 `/health` 仍答话并报 `local_db:false`，非空响应体 500 |

---

## 5. 建议的处理顺序

| 顺序 | 编号 | 理由 |
|---|---|---|
| 1 | BUG-01 | 每 20 分钟发生一次，已证明能把 71 分变成 90 分并吃掉计次 |
| 2 | BUG-02 | 直接等于白送免费餐，发生后只能人工作废 |
| 3 | BUG-07 | 一行配置即让整套实体卡在上线当天全废，修复只需两行 |
| 4 | BUG-04 · BUG-05 · BUG-06 | 均属「说了算的人不该是客户端 / 不该只有一个人」，改动都很小 |
| 5 | BUG-03 · BUG-08 | 当前影响有限，但都是将来一定会咬人的形状 |
| 6 | BUG-09 – BUG-16 | 留痕、一致性与文档，可合并为一个收尾提交 |

**并且：BUG-01 / 02 / 03 / 07 各需补一条断言。**
这四条都位于现有测试已经执行到的代码路径上，
只是没有断言检查它做对了没有 —— 每条的「应补断言」已在详情中写明。

---

## 6. 本轮未覆盖的范围

明确记录，以免误读本报告的结论强度：

* **`tests/e2e_pos.php`（95 项）未执行** —— 需可达的真实 POS 主库。
* **`tests/browser/*.mjs`（525 项）未执行** —— 需开发机图形环境。
  前端（`wwwroot/assets/pad.js` 等）本轮只做了代码审查，未做真浏览器验证。
* **真实 POS 数据分布未复验** —— 本轮 POS 主库是照 `pdb/pos_schema.sql`
  重建的空表加手工夹具。`docs/01` 中一切基于 88,616 行真实数据的结论
  （歧义率、金额恒等式、`status` 恒为 2 等）**本轮未复验，仍以 `docs/01` 原文为准**。
* **未做压力测试与长时间运行验证。** BUG-02 的并发结论来自 4 进程对齐调用，
  不代表对整体并发能力的评估。
* **未审查 `apk/` 容器侧**（不在本仓库内）。

---

*报告完 · 全部实测数据由 PHP 8.4.19 + MariaDB 10.11.14 环境产出。*
