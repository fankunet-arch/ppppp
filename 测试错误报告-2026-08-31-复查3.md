# 测试错误报告（第三次复查 + 新功能验收）· 2026-08-31

> **性质**：对上一轮 3 条问题的修复复查，**外加对本次新功能的验收检查**。
> **不改动仓库中任何既有文件**，涉及设计结论一律**引用** `docs/` 原文。
>
> 前三轮：[`2026-08-30`](./测试错误报告-2026-08-30.md)（16 条）·
> [`08-31 复查`](./测试错误报告-2026-08-31-复查.md)（+5 条）·
> [`08-31 复查2`](./测试错误报告-2026-08-31-复查2.md)（+3 条）
>
> **提交人**：网络系统测试员
> **被测版本**：`origin/main` @ `bfc11a9`
> **本轮改动**：`1257249`（修 R3 三条）+ `c8db10b`（**新功能**：结果页改版 + `points_mode`）
> **测试环境**：PHP 8.4.19 · MariaDB 10.11.14 · Node 22 · Chromium（Playwright）
> **POS 主库**：照 `pdb/pos_schema.sql` 重建的空表 + 手工夹具（**非真实门店数据**）

---

## 1. 一句话结论

**上轮 3 条全部修好。新功能本身的核心逻辑我逐条实测过，是对的 —— 但连带牵出 5 个问题，其中 2 个会影响到钱和日期。**

新功能（`points_mode = by_visit`「来一次积一分」）的设计做得相当完整：
后台置灰、Pad 措辞按口径切换、手工录入的例外、冲正不按比例、
以及专门写一段区分它和 `reward_mode` —— 这些**都不是事后补的，是一起想到的**。
`docs/03 §2.4` 那一节也写全了。

问题集中在两处：**一处是新函数照抄了老函数的浮点写法，把一个既有缺陷复制了一份**；
**另一处是新口径让几个既有配置项失去意义，但那些配置项的描述没跟着改**。
另外时区扫描顺带撞出一个跟本次改动无关的既有缺陷（券有效期跨夏令时差一天）。

---

## 2. 基线

| 套件 | 上轮 | 本轮 | 结果 |
|---|---|---|---|
| `tests/run.php` | 931 | **948** | 全部通过 |
| `tests/smoke.php --fresh` | 486 | **503** | 全部通过（⚠️ 见 [N4-01](#n4-01)：Pacific/Auckland 时区下红 1 项） |
| `tests/http_sweep.php` | 75 | **78** | 全部通过 |
| `tests/browser/*.mjs` | 391（15/20 文件） | **391**（15/20 文件） | 全部通过 |
| `tests/e2e_pos.php` | 未执行 | 未执行 | 需可达的真实 POS 主库 |

---

## 3. 上轮 3 条的复查 —— 全部修好

| 编号 | 结论 | 复验方式 |
|---|---|---|
| R3-01 降级停在登录那一步 | ✅ **已修复** | `card_prefix=VIP` 时找单 / 小票查单 / 手工录入**全部可用**；查卡仍停用（符合预期） |
| R3-02 装饰性的 `readonly` | ✅ **已修复** | 后台改 `pos_clock_offset_sec` 被拒；类型换成 `int_signed`，负值通过校验；`cp.js` 也认了 |
| R3-03 依赖钟点的假绿断言 | ✅ **已修复** | 四个时区（NY 07:56 / UTC 11:56 / Madrid 13:57 / Shanghai 19:57）跨度告警全绿 |

**R3-01 的实测**

```
  /auth/login             ✓ 登录成功 | cards_ok = false
  按桌号找单     /api.php/order/locate          ✓ 可用
  按小票号找单   /api.php/order/locate-invoice   ✓ 可用
  手工录入       /api.php/points/manual         ✓ 可用
  查卡           /api.php/card/status           ❌ 500（卡片功能确实停用）
```

`bin/init.php repair` 的提示语也改成了实情：

```
  ✗ config.card_prefix = VIP —— 不能含 I / L / O / U
     卡号跟自己对不上：实体卡的【查卡 / 建卡 / 激活 / 换卡】全部停用。
     记积分不受影响（找单、记账、手工录入照常），但客人手里那张卡
     查不了也激活不了 —— 等于会员体系只剩下已经绑好的那些人能用。
```

上一轮那句「积分照常记」是错的，这一版把「什么还能用、什么不能用」分开写了。

`App::cardNumberOrNull()` 这个做法也对：**需要卡号的地方照旧让它抛，
不需要的地方拿 null**，而不是到处 try/catch。

---

## 4. 新功能验收：`points_mode = by_visit`

### 4.1 核心行为 —— 逐条实测，都对

同一位客人、同一餐期的两张单（47.80 + 23.90），`once_per_period` 口径：

```
════ points_mode = by_amount（每欧元 1 分，倍率 1.0）════
  单 …0001 金额 47.80 → 计次 1，积分 47
  单 …0002 金额 23.90 → 计次 0，积分 23      ← 不计次但照样给分
  会员余额：积分 70 / 计次 1 / 累计消费 71.70
  手工录入 50.00 → 积分 50

════ points_mode = by_visit（每次 1 分，倍率 1.0）════
  单 …0001 金额 47.80 → 计次 1，积分 1
  单 …0002 金额 23.90 → 计次 0，积分 0       ← 没计次就没分（定义如此）
  会员余额：积分 1 / 计次 1 / 累计消费 71.70
  手工录入 50.00 → 积分 1                     ← 按【一次】给，计次仍 0
```

其余路径也逐个验过：

| 场景 | 实测结果 | 判断 |
|---|---|---|
| 均摊 AA（4 人分一张 4 份的单） | 四人各 计次 1 / 积分 1 | ✓ |
| 多桌合并（三桌 167.30） | 第 1 桌 1 次 1 分，第 2、3 桌 0 次 0 分；合计 1 分 | ✓ 与「并积分不并次数」一致 |
| 手工撤销 `/points/reverse` | 分和次都退干净（0 分 / 0 次） | ✓ |
| 十送一进度 | `reward_mode=visits` 看 `visit_count`；`=amount` 看 `total_spent` | ✓ 与积分口径无关 |
| Pad 结果页（真浏览器） | 大字「+1 次」；0 次时变灰；两种口径的橙字措辞不同 | ✓ |
| 后台置灰 | `points_per_euro` / `points_per_visit` 各自带 `active_when` | ✓ |
| `/pad/settings` | 带回 `points_mode` | ✓ |

Pad 结果页的真浏览器输出：

```
  points_mode = by_amount
    大字 "+1 次"            小字 "TK-00000275-1A2 · € 47.80 · +47 分"
    大字 "+0 次"（灰）      橙字 "本餐期已记过 1 次，这一单只记积分不计次"

  points_mode = by_visit
    大字 "+1 次"            小字 "TK-00000275-1A2 · € 47.80 · +1 分"
    大字 "+0 次"（灰）      橙字 "本餐期已记过 1 次 —— 按次数积分，所以这一单不计次也不加分"
```

两种口径下那句橙字确实不一样 —— 这一条如果漏了，收银员照着屏幕念出来的就是错的。
西语两句也都在。

### 4.2 顺带确认

`points_mode` 与 `reward_mode` 的区分（`docs/03` §2.4 末尾专门写了一段）是必要的：
两者都有「按次数 / 按金额」的说法，实测确认改 `points_mode` **不会**动任何人的发券进度。

---

## 5. 本轮新发现

| 编号 | 级别 | 摘要 | 位置 | 性质 |
|---|---|---|---|---|
| [N4-01](#n4-01) | 🟠 中 | 券有效期跨夏令时差一天；某些时区下 smoke 直接红 | `RewardService.php:344` | 既有缺陷，本轮由时区扫描撞出 |
| [N4-02](#n4-02) | 🟠 中 | `floor(a×b×c)` 在精确值本是整数时少给 1 分 | `PointsEngine::pointsForVisit` / `pointsFor` | 旧的既有，**新函数复制了一份** |
| [N4-03](#n4-03) | 🟡 低 | `by_visit` 的冲正要「缩到正好 0」才收回；计次从来不收回 | `ReconcileService::applyShrink` | 本次新增 + 既有 |
| [N4-04](#n4-04) | 🟡 低 | `free_meal_extra_earns` 在 `by_visit` 下失效，描述没改 | `ConfigSchema.php` | 本次新增带出 |
| [N4-05](#n4-05) | 🟡 低 | `by_portion` × `by_visit` 相乘，「来一次积几分」的标签不成立 | `ConfigSchema.php` | 本次新增带出 |

---

<a id="n4-01"></a>
### N4-01 · 🟠 中 · 券有效期跨夏令时差一天

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/Service/RewardService.php:344`（`issue()`） |
| **性质** | 既有缺陷，与本次改动无关。本轮把 smoke 换到五个时区跑才撞出来 |
| **引用文档** | `docs/09`、`docs/11` —— 券面/卡面上印的日期是**唯一的告知证据** |

**怎么发现的**

复验 R3-03（时间敏感断言）时，我把 smoke 在五个时区各跑一遍。
跨度告警那条四个时区全绿 ✓，但 **Pacific/Auckland 23:57 换了另一条断言红**：

```
  America/New_York   本地 07:56   跨度告警 ✓   冒烟测试通过
  UTC                本地 11:56   跨度告警 ✓   冒烟测试通过
  Europe/Madrid      本地 13:57   跨度告警 ✓   冒烟测试通过
  Asia/Shanghai      本地 19:57   跨度告警 ✓   冒烟测试通过
  Pacific/Auckland   本地 23:57   跨度告警 ✓   失败 1
                                               ↑ ㉑「改完之后【新发的】才是 30 天」
```

**根因**

```php
$to = $validDays > 0
    ? date('Y-m-d', strtotime($now) + $validDays * 86400)   // ← 加的是秒，不是日历天
    : null;
```

`+N × 86400 秒` 在跨夏令时切换时与「+N 个日历天」相差一小时，
足以把日期推过午夜。

**实测**（`coupon_valid_days` 出厂默认 90 天）

```
  时区              发券时刻              代码算出      日历天应为
  Europe/Madrid    2025-12-27 23:30:00   2026-03-27   2026-03-27   ✓
  Europe/Madrid    2026-07-29 00:30:00   2026-10-26   2026-10-27   ❌ 差一天
  Europe/Madrid    2026-07-29 23:30:00   2026-10-27   2026-10-27   ✓
  Europe/Madrid    2026-06-15 20:00:00   2026-09-13   2026-09-13   ✓（不跨切换）
  Pacific/Auckland 2026-08-31 23:57:00   2026-10-01   2026-09-30   ❌ 差一天
```

**为什么在这家店会真的发生**

晚市餐期是 **19:30–02:00**（`db/seeds/002_meal_period.sql`），
所以 **00:xx 发券是常态**，不是边角。于是每年十月回跳前的 90 天里，
00:00–00:59 之间发出的券会**少一天**；三月前跳前的 90 天里，
23:00–23:59 发出的券会**多一天**。

`docs/11` 反复强调「卡面/券面上印的日子就是最终的日子」——
少一天正好落在这条承诺的反面。

**顺带查过**：全仓库只有这一处把 `86400` 当日历天用。
`PointsService:139`（回溯天数）和 `AuthService:303`（会话清理宽限）
都是时间戳比较，差一小时无影响；`BusinessDay::range()` 上一轮已经改成日历天了。

**建议**

```php
$to = (new \DateTimeImmutable($now))->modify("+{$validDays} days")->format('Y-m-d');
```

**应补断言**：`smoke ㉑` 换到一个当前跨 DST 的时区跑一遍；
或者直接断言 `valid_to === (new DateTimeImmutable(created_at))->modify("+N days")`。

---

<a id="n4-02"></a>
### N4-02 · 🟠 中 · 精确值本是整数时少给 1 分

| 项 | 内容 |
|---|---|
| **位置** | `PointsEngine::pointsForVisit()`（**本次新增**）与 `pointsFor()`（既有） |
| **性质** | 旧函数一直有；新函数照抄了同一个写法 |

**根因**

两个函数都是 `(int)floor($a * $b * $c)`，三个 IEEE754 双精度相乘。
当精确结果本该是整数时，浮点表示会给出比它略小的值，`floor` 就少一分。

代码注释写的是：

> 向下取整而非四舍五入：宁可少给，避免累积超发。

那是一条关于**小数部分**的取整策略，是对的。
但这里丢的不是小数 —— **精确值就是 29，代码给 28**。这是表示误差，不是策略。

**实测**（穷举后台可填的两位小数组合）

```
  pointsForVisit（新增）：扫 1,080,000 种组合，24 种算错
    次数 12 × 0.70 × 2.50 → 代码给 20，精确值 21
    次数  9 × 1.20 × 2.50 → 代码给 26，精确值 27
    次数 12 × 1.40 × 2.50 → 代码给 41，精确值 42

  pointsFor（既有，by_amount）：扫 200,000 种组合，63 种算错
    金额 100.00 × 0.29 × 1.00 → 代码给 28，精确值 29     ← 出厂倍率 1.0
    金额 100.00 × 0.29 × 2.00 → 代码给 57，精确值 58
    金额 100.00 × 0.25 × 1.16 → 代码给 28，精确值 29
```

`points_per_euro = 0.29` 不是刁钻取值 —— 店家想设「3.45 € 换 1 分」时算出来就是它，
而 `points_multiplier` 出厂就是 `1.0`。**这一档不需要任何特殊配置就会命中。**

**影响**：每次少 1 分，方向始终对商家有利，客人发现不了。
金额越大命中越密（`pointsFor` 的分母是 100×100×100）。

**建议**

两个函数都改走整数运算，避免中间出现浮点：

```php
// 把 perEuro / perVisit / multiplier 先按两位小数转成整数（× 100），
// 最后一次性整除 —— 与 Money 里「一律以整数分流转」是同一条原则
return intdiv($baseCents * $perEuroX100 * $multX100, 100 * 100 * 100);
```

`Money` 类开头那句「金额一律以整数分在内部流转，避免浮点误差」，
这两个函数是仓库里仅剩的两处例外。

**应补断言**：`PointsEngineTest` 加一条 `points_per_euro=0.29 / 100.00 € → 29`。

---

<a id="n4-03"></a>
### N4-03 · 🟡 低 · `by_visit` 的冲正要「缩到正好 0」才收回；计次从来不收回

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/Service/ReconcileService.php` `applyShrink()` |
| **引用文档** | `docs/03` §6.4 |

新增的分支是：

```php
$backPts = $pointsMode === 'by_visit'
    ? ($backAmt >= $amt ? $pts : 0)          // 整条退干净才收回
    : (int)ceil($pts * ($backAmt / max($amt, 1)));
```

「不能按比例退」的判断是对的（`ceil(1 × 任意正比例)` 恒等于 1，
退五分钱也会把整次的分抹掉）。但 `$backAmt >= $amt` 这个铰链**要求订单缩到正好 0**：

```
  95.60 → 0.00（正好 0） 记账后 1 分/1 次 → 冲正后 0 分/1 次 （分已收回）
  95.60 → 0.01           记账后 1 分/1 次 → 冲正后 1 分/1 次 （分【没】收回）
  95.60 → 4.78（缩 95%） 记账后 1 分/1 次 → 冲正后 1 分/1 次 （分【没】收回）
```

一张缩掉 99.99% 的单，客人保住那一分。按注释的定义（「部分缩水不动分」）这是**代码符合意图**，
所以不算实现错误 —— 但那个意图在 0.01 这个刻度上是否还成立，值得店家自己决定。

**顺带一条更值得看的**：`counted_visit => 0` 意味着
**值比对路径在两种口径下都不收回计次**。上面第一行就是证据：
订单归零后积分退了，**十送一那个格子还在**。而免费餐才是真花钱的东西。

对照手工撤销：

```
  /points/reverse → 撤销后 0 分 / 0 次   ✓ 分和次都退干净
```

这条不是本次引入的（注释说「金额缩水不改变吃了几份套餐」），
但在 `by_visit` 下变得刺眼：分和次本来就是同一件事，退了一个不退另一个。

**建议**：把铰链从「正好 0」放宽（例如新总额低于一份套餐单价即视为整单作废）；
并考虑在订单归零时一并收回计次。

---

<a id="n4-04"></a>
### N4-04 · 🟡 低 · `free_meal_extra_earns` 在 `by_visit` 下失效，而描述没改

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/ConfigSchema.php` `free_meal_extra_earns` 的 `desc` |

该开关的后台描述写着：

> 关（默认）：整单都不给。**开：套餐部分不给，额外消费照常给。**

在 `by_visit` 下这句话不成立 —— 免费餐/核销单 `$freeish` ⇒ 计次 0 ⇒ 积分 0，
不管额外消费多少。

**实测**（一张十送一核销单，另点了 12.00 的酒水）

```
  extra_earns=0  by_amount  可积分额 12.00 → 拒绝 redeemed
  extra_earns=0  by_visit   可积分额 12.00 → 拒绝 redeemed
  extra_earns=1  by_amount  可积分额  4.01 → 积分 4 / 计次 0     ← 开关生效
  extra_earns=1  by_visit   可积分额  4.01 → 积分 0 / 计次 0     ← 开关无效
```

这不是计算错误（是 `by_visit` 的定义），但**后台会让店家以为开了就有用**。
本次已经给 `points_per_euro` 加了 `active_when` 置灰 —— 同一套机制这里没用上。

**建议**：给它加 `active_when: points_mode = by_amount`，或在描述里补一句
「按次数口径下这一项无效（没计次就没有分）」。

---

<a id="n4-05"></a>
### N4-05 · 🟡 低 · `by_portion` × `by_visit` 相乘，标签不成立

| 项 | 内容 |
|---|---|
| **位置** | `app/lib/ConfigSchema.php` `points_per_visit` 的 label「来一次积几分」 |

`points_mode` 与 `visit_count_mode` 是两个正交的开关，而 `by_visit` 的分**直接乘以计次数**：

```
  visit_count_mode=once_per_period  一张 4 份的单 → 计次 1，积分 1
  visit_count_mode=by_portion       一张 4 份的单 → 计次 4，积分 4    ← 变成「一份一分」
  visit_count_mode=by_order         一张 4 份的单 → 计次 1，积分 1
```

`docs/03` §13 已经警告过 `by_portion` 在十送一那侧的后果
（「一张 10 人的小票一次顶 10 次计次」）；换成 `by_visit` 之后，
同一张小票现在还**顺带给 10 分**。两个开关的风险相乘了。

标签写的是「**来一次**积几分」，而在这个组合下是「**一份**积几分」。

**建议**：`points_per_visit` 的描述里点明它乘的是计次数、
以及 `visit_count_mode` 会改变它的含义；
或者在后台把 `by_portion` + `by_visit` 这个组合标成需要确认。

---

## 6. 建议的处理顺序

| 顺序 | 编号 | 理由 |
|---|---|---|
| 1 | N4-02 | 出厂倍率 1.0 + 一个正常的每欧元分数就会命中，且客人发现不了；两个函数一起改 |
| 2 | N4-01 | 客人手里那张券上的日期是唯一告知证据；改动是一行 |
| 3 | N4-04 · N4-05 | 都是「新口径让老配置变了意思」，一起补描述/置灰即可 |
| 4 | N4-03 | 需要店家先定「缩到多少算作废」，属于业务决策而非代码缺陷 |

---

## 7. 本轮未覆盖的范围

* **`tests/e2e_pos.php`（95 项）未执行** —— 需可达的真实 POS 主库。
* **`tests/browser/` 有 5 个文件未执行**（`assignlock` / `bindamount` / `locate` /
  `merge` / `visitcount`）—— 需 `tests/sim/live_orders.json`。
  ⚠️ 其中 `visitcount.mjs` 与 `bindamount.mjs` 本次都改动过（钉死 `points_mode=by_amount`、
  选座跳过锁定行），**这两个文件的改动本轮没能实际跑到**。
* **真实 POS 数据分布未复验** —— `docs/01` 中基于 88,616 行真实数据的结论
  本轮未复验，**仍以 `docs/01` 原文为准**。
* **`by_visit` 的长期行为未验证** —— 只验了单次记账/撤销/冲正/合并/AA 这几条路径。
  切换口径之后老流水的解释（历史上按金额发的分与新发的分混在一个余额里）没有测，
  也没有测「切换口径」本身是否需要迁移动作。
* **未做压力测试**，**未审查 `apk/` 容器侧**（不在本仓库内）。

---

*报告完 · 全部实测数据由 PHP 8.4.19 + MariaDB 10.11.14 + Chromium 环境产出。*
