# 测试错误报告（第七次复查 · Z-01 验收 + 回归排查）· 2026-09-02

> **本轮命题**（来自委托方）：**做了一次修复，检查是否符合要求，以及是否带入了新的 bug。**
>
> **不改动仓库中任何既有文件**，涉及设计结论一律**引用** `docs/` 与源码原文。
>
> 前七轮：[`08-30`](./测试错误报告-2026-08-30.md)（16）·
> [`复查`](./测试错误报告-2026-08-31-复查.md)（+5）·
> [`复查2`](./测试错误报告-2026-08-31-复查2.md)（+3）·
> [`复查3`](./测试错误报告-2026-08-31-复查3.md)（+5）·
> [`复查4`](./测试错误报告-2026-08-31-复查4.md)（+3）·
> [`复查5`](./测试错误报告-2026-09-01-复查5.md)（+3）·
> [`复查6`](./测试错误报告-2026-09-02-复查6.md)（+1）
>
> **被测版本**：`origin/main` @ `f728c93`
> **本轮被复查的改动**：`549e56e`（Z-01 修复）+ `6dc8ba1`/`6f05449`/`dd9d64a`（自查 P0/P1/P2 共 15 条）
> + `a6a2cee`、`1b07594`、`e555d97`（自查续 + 自己引入的死锁）
> **提交人**：网络系统测试员
> **测试环境**：PHP 8.4.19 · MariaDB 10.11.14 · `php -S` 开 8 个 worker
> **POS 主库**：照 `pdb/pos_schema.sql` 重建的空表 + 手工夹具（**非真实门店数据**）

---

## 1. 一句话结论

**Z-01 修得对，而且把我担心的两处副作用都躲开了。但这一批改动带进来一个新的 bug —— 而它正是这个仓库反复踩的那一类：修了那一处、没修那一类。**

| | 结论 |
|---|---|
| **Z-01 是否符合要求** | ✅ **符合**，三个角度分别验过（本体 / 不误伤混合单 / 不重复扣） |
| **新引入的 bug** | 🔴 **一条**：[R-01](#r-01) 先记账 → 后核销 → 再撤销，客人的计次被扣两次，变成 **−1** |

R-01 一句话：核销时新插的那条「免费那一餐不计次」的负数流水，
**没有把原流水标掉**；随后撤销原流水时，`reverseInTx` 直接照着原流水上的
`counted_visit = 1` 又退了一次 —— **同一次被退两遍**。

而 `clawBackVisitOnRedeem()` 自己的注释就写着这句话：

> 不能是原流水上的 `counted_visit` —— 否则一单核销两张券时，
> 第二次核销会把同一次再退一遍，**把客人的次数扣成负的**。

他们为「一单两张券」这条路守住了，**撤销那条路没守** —— 同一个形状，隔了一个函数。

---

## 2. 基线

| 套件 | 上轮 | 本轮 | 结果 |
|---|---|---|---|
| `tests/run.php` | 988 | **1002** | 全部通过 |
| `tests/smoke.php --fresh` | 622 | **701** | 全部通过 |
| `tests/http_sweep.php` | 89 | **93** | 全部通过 |

三套全绿 —— **而 R-01 三套都照不到**，原因见 §4.4。

---

## 3. Z-01 验收：符合要求

### 3.1 本体：匹配串认不出时，用券那一餐不再攒一次

`redeem()` 现在带单号时会把事实写回订单（`OrderRepo::markRedeemedByApp`）。
用上一轮**完全相同**的场景（折扣行改名成「10+1 GRATIS」，匹配串认不出）：

```
① 第 10 次 → 计次 10，发券 1 张
② 核销：{"ok":true}
   订单现在：{"is_redeemed":1,"portions_counted":0}   ← 回写了
③ 服务员照常记这一单：{"ok":false,"error":"redeemed"}
   计次 10 ← ★ 没有再攒（修好了）
```

上一轮这里是「计次 11」。

### 3.2 不误伤混合单 —— 这是我最担心的一处

只写 `is_redeemed` 不扣份数会漏、顺手把份数清零又会误伤 4 人同桌里照付的那 3 位。
提交信息说扣的是**券的张数**。实测 4 人桌（份数 4）、其中 1 人用券：

```
核销后订单：{"is_redeemed":1,"portions_counted":3}   ← 只扣了一份

记其余两位（各 50.00 一份）：{"ok":true}
  C：计次 1 ← 正常计上了
  D：计次 1 ← 正常计上了

再记三位（净份数正好 3）：{"ok":true}
  C：计次 1   D：计次 1   A：计次 11
```

**没有误伤。**`docs/03 §5.5` 那条「静默少给」没有回来。

### 3.3 不会扣两次 —— 那条 MySQL 求值顺序的假设成立

`markRedeemedByApp` 依赖「`UPDATE` 的赋值从左到右求值，
`portions_counted` 排在 `is_redeemed` 前面才读得到旧值」。
这是一条**数据库行为假设**，值得单独验：

```
初始：      {"is_redeemed":0,"portions_counted":4}
第一次回写：{"is_redeemed":1,"portions_counted":3}
第二次回写：{"is_redeemed":1,"portions_counted":3}   ← ★ 没有扣两次
```

在 MariaDB 10.11 上成立。

### 3.4 新告警 `redeem_unflagged` 不误报

```
已核销且带单号的券：1 张；其中所在订单【没标核销】的：0 张
checkIntegrity 结论：（没有 redeem_unflagged）
```

用 `INNER JOIN`、只查本地镜像的做法是对的 —— 订单不在镜像里不算漏。

---

<a id="r-01"></a>
## 4. R-01 🔴 新引入：先记账 → 后核销 → 再撤销，客人的计次被扣两次

### 4.1 现场是怎么发生的

三步都是柜台上的日常动作，一步都不出格：

1. **服务员先按 AA 把这桌两位都记了**（正常记账）；
2. **客人结完账才想起「我有张券」**，服务员把券核销到这一单上
   —— `/coupon/redeem` 的 `serial_id` 参数本来就是为这件事准备的；
3. **经理发现这张单记错了卡**，撤销那一笔（正常撤销）。

结果：**这位客人的计次变成 −1**。

### 4.2 实测（走真实 HTTP 接口）

```
════ 走真实 HTTP：先记账 → 后核销 → 再撤销 ════
登录： True
① 记账（A、B 各 50.00 一份）： ok= True
   TK000G0001ABC  计次 1
   TK000G0002ABC  计次 1
② B 拿出券，核销到这一单上： ok= True
   TK000G0001ABC  计次 1
   TK000G0002ABC  计次 0        ← 这一步是对的：免费那一餐不该攒
③ 经理发现这张单记错了，撤销 B 那一笔（流水 #114）： ok= True
   TK000G0001ABC  计次 1
   TK000G0002ABC  计次 -1       ← ★ 同一次被退了两遍
```

逐条流水看得更清楚：

```
① 记账后        流水#109 会员2 类型1 计次 1  状态1
② 核销回写后    流水#110 会员2 类型2 计次-1  状态1   ← 新插的补偿流水
                流水#109 仍然是 状态1、计次 1        ← 原流水【没有被标掉】
③ 撤销#109      流水#112 会员2 类型2 计次-1  状态1   ← 又退了一次
                → 会员计次 = 1 + (-1) + (-1) = -1
```

### 4.3 根因：两条路读的不是同一个数

`RewardService::clawBackVisitOnRedeem()`（本次新增）算的是
**「这位客人在这一单上现存的净次数」**，所以「一单两张券」不会退两遍：

```php
// 净次数：原流水的正数 + 以前退过的负数
$net += (int)$e['counted_visit'];
```

而 `PointsService::reverseInTx()`（既有）读的是**原流水上那个数**：

```php
$visits = (int)$orig['counted_visit'];      // ← 1
...
'counted_visit' => -$visits,
$this->members->applyDelta($memberId, -$points, -$visits, -$amt);
```

补偿流水 `#110` 把那一次退掉时，**没有把 `#109` 标成已冲正**
（它用的是 `reverses_id = anchor`，但 `#109` 的 `status` 仍是有效）。
于是 `#109` 还能被正常撤销，而撤销时看到的仍是「计次 1」。

**同一个形状，`clawBackVisitOnRedeem` 守住了，`reverseInTx` 没有。**
这正是 `docs/13 §3.1`「修了那一处、没修那一类」——
而且这一次连注释都提前写出来了：

> 否则一单核销两张券时，第二次核销会把同一次再退一遍，**把客人的次数扣成负的**。

### 4.4 为什么三套测试都没抓到

`tests/smoke.php` 里 F4 那一组把「一单再核销一张券」的幂等钉得很死
（第 4506–4512 行），但**它到此为止 —— 没有再撤销那条原流水**：

```php
// 幂等：同一单再核销一张券，不能把同一次再退一遍
$fxApp->rewards()->redeem($f4C2, '4400000004', $fxOp, null, ['reason' => '冒烟']);
eq($f4V0, (int)$fxApp->members()->findById($f4M)['visit_count'], … );
```

而 5051 行那条总不变量 `★ 没有负积分 / 负计次的会员（冲正不该把人扣穿）`
本来正好能抓住它 —— **只是场景没走到那一步**：
suite 里没有任何一处把「核销回写」与「撤销」串在一起。

### 4.5 后果

| | |
|---|---|
| 谁吃亏 | **客人**。计次少一次，下一顿免费餐要多来一趟 |
| 界面上怎么显示 | `progressOf` 里 `remain = (earned+1) * threshold − progress`，
progress 是负数 → 门槛 3 时进度条会写「还差 **4** 次」，比门槛本身还大 |
| 会不会连累账 | 不会。流水合计与会员表一致（1 −1 −1 = −1），金额与券都对；坏的只有计次 |
| 触发条件 | **不需要任何异常操作** —— 只要「先记账、后核销、再撤销」这三步都是常规动作 |

顺带记一条同源的观察（**不单列为缺陷**）：核销回写把 `portions_counted` 从 2 改成 1 之后，
`allocated_portions` 仍然是 2，出现 `已分配(2) > 净份数(1)` 的倒挂。
后续再往这张单上记人会被 `too_many_members` 正确拒掉，没有引发别的问题，
但这个中间状态本身是不自洽的。

### 4.6 修的方向（供参考，我不改代码）

两条都能解，选一条即可：

* **A（小改）**：`reverseInTx` 也改成读「这位客人在这一单上现存的净次数」，
  与 `clawBackVisitOnRedeem` 用同一个判据 —— 两处用同一套算法，
  正是 `docs/13 §3.5`「前后说两套话」那一节要求的；
* **B（更彻底）**：补偿流水写完之后，把原流水上那一次**标记为已退**
  （或直接把 `#109` 的 `counted_visit` 归零并留痕），
  让「原流水上那个数」永远等于「现存的净次数」，后面谁读都不会错。

无论选哪条，建议在 smoke F4 后面直接接一步撤销 ——
5051 行那条总不变量会立刻把它钉住。

---

## 5. 其余改动逐项撞过，没有发现回归

这一批一共改了 15+ 条，我按「最可能撞坏什么」挑了四处压：

### 5.1 新加的手工额度互斥锁（迁移 018）—— ✅ 没问题

他们自己在 `e555d97` 里发现第一版的区间锁制造了死锁，改成单行 X 锁。
我独立压了一遍：**2 个手工进程 + 2 个记账进程，各 40 笔**。

```
manual   MA   成功 8   撞上限 32  死锁 0
manual   MB   成功 7   撞上限 33  死锁 0
grant    GA   成功 40  撞上限 0   死锁 0
grant    GB   成功 40  撞上限 0   死锁 0
──────────
手工录入实际入账合计：€ 300.00（上限 300.00）
Innodb_deadlocks    0
```

**一分不多、一次不死锁，而且正常记账 80/80 完全没被挡住** ——
单行锁没有 gap、记账那条路根本不碰它，这个修法是对的。

另外 `ensureQuotaRow()`（`INSERT IGNORE`，在事务外）+ `lockManualQuota()`（事务内 `FOR UPDATE`）
的顺序也是对的 —— 不存在「那一行碰巧不存在、锁了个寂寞」的老问题。

### 5.2 `sameSitting` 重写 —— ✅ 八个边界全对

它决定 `once_per_period` 给不给第二次，直接关系到计次。按出厂餐期
（11:00–18:00、19:30–02:00，中间是空档）实测：

```
午市内两单          true  （同一顿，不再计次）
午市 vs 空档        false （不同顿，会再计一次）
空档内两单          true
空档 vs 晚市        false
晚市内两单          true
晚市跨零点（同营业日）true
午市 vs 晚市        false ✓  ← 中午来一次、晚上又来一次，应当是两次
跨营业日（切点两侧）false
```

空档自成一格、晚市跨零点仍算同一顿，都对。

### 5.3 新的 `zero_allocation` 拒绝 —— ✅ 不挡正常生意

我第一次构造混合单时故意带了一行「0 元 0 份」，被它拒了 —— 那是对的。
按 Pad 实际提交的内容（提交前已滤掉零行）重跑，正常通过：

```
记 C/D 两位（各 50.00 一份）：{"ok":true}
```

### 5.4 `redeem_unflagged` 新告警 —— ✅ 不误报

见 §3.4。

---

## 6. 一句话给老板

> **Z-01 修对了**，而且把最容易踩的两处（误伤混合单、重复扣份数）都躲开了；
> 顺手做的那批自查，我挑了四处最可能撞坏的地方压过，都没问题 ——
> 尤其新加的那把额度锁，160 笔一次没死锁、一分钱没超。
>
> **但带进来一个新 bug**：客人「先记账、后拿出券、再撤销」这三步都是日常动作，
> 走完之后他的计次会变成 **−1** —— 同一次被退了两遍。
> 而这一条的注释里已经写着「会把客人的次数扣成负的」，
> 只是守在了另一个函数里。**smoke 里那条「不许有负计次」的总不变量本来能抓住它，
> 差的只是把「核销」和「撤销」这两步接起来。**

---

## 7. 附：本轮用到的复现脚本

都在会话临时目录下，**没有进仓库**：

| 脚本 | 用途 |
|---|---|
| `z1_probe.php` / `z1b_probe.php` | Z-01 验收：本体、混合单不误伤、回写不扣两次 |
| `neg2_probe.php` | **R-01**：逐条流水看清计次是怎么被退两遍的 |
| `neg_setup.php` + curl | **R-01**：走真实 HTTP 的三步复现 |
| `cap_setup.php` / `cap_worker.php` | 新额度锁的并发压测（2 手工 + 2 记账 × 40 笔） |
| `ms_probe.php` | `sameSitting` 的八个餐期边界 |
| `wb_probe.php` | 先记账后核销时 `allocated_portions` 的倒挂 |
| `ru_probe.php` | `redeem_unflagged` 是否误报 |

---

*本报告只新增本文件，未改动仓库中任何既有文件。*
