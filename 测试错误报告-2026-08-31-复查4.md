# 测试错误报告（第四次复查 · 按「会不会影响生意 / 亏钱 / 招投诉」定向排查）· 2026-08-31

> **本轮命题**（来自委托方）：**还有没有会致命影响生意、会让店铺造成损失、
> 或者会造成不必要的麻烦而被客户投诉的 bug。**
> 所以这一轮不做面面俱到的全量审计，只沿这三条线打。
>
> **不改动仓库中任何既有文件**，涉及设计结论一律**引用** `docs/` 与源码原文。
>
> 前四轮：[`08-30`](./测试错误报告-2026-08-30.md)（16 条）·
> [`08-31 复查`](./测试错误报告-2026-08-31-复查.md)（+5 条）·
> [`08-31 复查2`](./测试错误报告-2026-08-31-复查2.md)（+3 条）·
> [`08-31 复查3`](./测试错误报告-2026-08-31-复查3.md)（+5 条）
>
> **提交人**：网络系统测试员
> **被测版本**：`origin/main` @ `620277d`
> **本轮新改动**：`8382d8d`「这一单不计次」改在记账之前说（新增 `PointsService::visitPreview()`）
> **测试环境**：PHP 8.4.19 · MariaDB 10.11.14 · `php -S` 开 8 个 worker（**这一条很重要，见 §7**）
> **POS 主库**：照 `pdb/pos_schema.sql` 重建的空表 + 手工夹具（**非真实门店数据**）

---

## 1. 一句话结论

**三条线各命中一发，两发是硬伤。**

| 编号 | 一句话 | 影响生意 | 店铺亏钱 | 客户投诉 |
|---|---|:--:|:--:|:--:|
| [N5-01](#n5-01) | 高峰期两台 Pad 同时记 AA 单会撞 MySQL 死锁，收银台弹「数据库不可用，请联系管理员」 | 🔴 | — | 🔴 |
| [N5-02](#n5-02) | 撤销记账不回收已发出的十送一券，而且**连客人后来真正挣到的那张也一并吞掉** | — | 🔴 | 🔴 |
| [N5-03](#n5-03) | 新增的「不计次」预览，免费餐那一支说的话在出厂配置下永远兑现不了 | — | — | 🟡 潜伏 |

N5-01 与 N5-02 都**不是边角情况**：前者只要门店有两台 Pad、两位客人 AA 分单，
就会按概率发生；后者只要有人扫错卡再撤销，就必然发生，且**店里没有任何界面看得到它**。

本轮新功能（`visitPreview`）本身的主干逻辑是对的 —— 复用 `visitsFor()` /
`countedThisSitting()` 这个决定尤其对，我专门验过预览与实际入账走的是同一条判断。
问题只在它顺手多写的那一支免费餐分支上（N5-03，目前打不到，但话已经说错了）。

---

## 2. 基线

| 套件 | 上轮 | 本轮 | 结果 |
|---|---|---|---|
| `tests/run.php` | 948 | **967** | 全部通过 |
| `tests/smoke.php --fresh` | 503 | **537** | 全部通过 |
| `tests/http_sweep.php` | 78 | **82** | 全部通过（⚠️ 见 §8 夹具说明） |

三套基线全绿，与提交信息里写的 `run 967 / smoke 537 / http 82` 完全对得上。
**本轮三条问题没有一条能被现有测试照到** —— 原因分别在各条的「为什么测试没抓住」里写了。

---

<a id="n5-01"></a>
## 3. N5-01 🔴 高峰期两台 Pad 同时记 AA 单 → MySQL 死锁 → 收银台报「数据库不可用，请联系管理员」

### 3.1 现场是怎么发生的

晚市，两桌客人。两位老顾客（同一对夫妻、或者常一起来的两个朋友）
分别在两桌上都出现了 —— 这在小店里天天有。

- 1 号 Pad 记 A 桌：先点了张三，再点李四
- 2 号 Pad 记 B 桌：先点了李四，再点张三

**只差一个点人的顺序。** 两台 Pad 谁也没做错事。

结果：其中一台的收银员看到

```
本地数据库暂时不可用，请联系管理员（错误代码 E109-4EDFC7）
```

客人就站在柜台前面。收银员现在要么去打电话找管理员，要么当着客人的面重来一遍
—— 而这个错误**再点一次就好了**，只是界面上完全没有这么说。

### 3.2 根因：`grant()` 忘了排序，而 `grantMerged()` 记得

`app/lib/Service/PointsService.php` — 合并记账那条路**专门为这件事做了防护**：

```php
// ★ 固定加锁顺序，防止并发合并时死锁
sort($serials);
```

而普通记账这条路，`grantOne()` 是按【客户端给过来的顺序】逐个锁会员行的：

```php
private function grantOne(string $serialId, array $allocations, int $allocMode,
                          array $operator, ?string $group): array
{
    $order = $this->orders->lockBySerial($serialId);   // ① 先锁订单行
    ...
    foreach ($allocations as $a) {
        $memberId = (int)$a['member_id'];
        ...
        $member = $this->members->lockById($memberId);  // ② 按【客户端给的顺序】锁会员行
```

而 `allocations` 的顺序，就是**服务员在 Pad 上点人的顺序**。
`grant()` 从头到尾没有对它排过序。

`grantMerged()` 的那句 `sort()` 说明这个风险是**被想到过的**，只是没有推广到
普通记账 —— 而普通记账才是每天跑几百次的那条路。

### 3.3 实测：走真实 HTTP 接口，两台 / 四台 Pad

两个 worker 对齐同一个起跑时刻，各自 POST `/api.php/points/grant`，
两位会员固定，**只把 allocations 的顺序反过来**。

**两台 Pad，各 120 单：**

```
HA   成功 118 / 120
     ×1  503|db_unavailable|本地数据库暂时不可用，请联系管理员（错误代码 E109-4EDFC7）
     ×1  503|db_unavailable|本地数据库暂时不可用，请联系管理员（错误代码 E109-C54C8E）
HB   成功 119 / 120
     ×1  503|db_unavailable|本地数据库暂时不可用，请联系管理员（错误代码 E109-989AFE）
```

→ 3 / 240 ≈ **1.25%**

**四台 Pad，各 80 单：**

```
P1   成功 35 / 80
P2   成功 41 / 80
P3   成功 16 / 80      ← 五单里四单记不进去
P4   成功 49 / 80
```

→ 141 / 320，**失败 179 单 = 55.9%**

**不是我猜的，是 MySQL 自己数的：**

```
mysql> SHOW GLOBAL STATUS LIKE 'Innodb_deadlocks';
Innodb_deadlocks    182
```

两轮实测一共 182 次失败（3 + 179），`Innodb_deadlocks` 正好 182 —— **一次不多，一次不少**。
每一次失败都是死锁，没有别的成分。

InnoDB 自己记下的那一条：

```
LATEST DETECTED DEADLOCK
*** (1) TRANSACTION:
SELECT * FROM member WHERE store_code = ? AND id = ? FOR UPDATE
*** WAITING FOR THIS LOCK TO BE GRANTED:
RECORD LOCKS ... index PRIMARY of table `vip_live`.`member` trx id 34209 lock_mode X waiting
*** (2) TRANSACTION:
SELECT * FROM member WHERE store_code = ? AND id = ? FOR UPDATE
*** WAITING FOR THIS LOCK TO BE GRANTED:
RECORD LOCKS ... index PRIMARY of table `vip_live`.`member` trx id 34206 lock_mode X waiting
*** WE ROLL BACK TRANSACTION (2)
```

两条事务，同一句 `MemberRepo::lockById()`，各自攥着对方要的那一行。教科书式的加锁顺序死锁。

### 3.4 三个让它更糟的地方

**① 错误分类里没有死锁这一档，于是被归成「数据库不可用」**

`app/lib/Http/Api.php::classify()` 的驱动码分支：

```php
return match ($drv) {
    2002, 2003, 2006 => 'E101',   // 连不上
    1045             => 'E105',   // 口令错
    1044, 1049       => 'E107',   // 库不存在
    1040, 1203       => 'E108',   // 连接数打满
    default          => str_starts_with($state, '08') ? 'E101' : 'E109',
};
```

死锁是 **1213 / SQLSTATE 40001**，一个都没列上 → 落到 `E109`。
再往下一句：

```php
$isDb = $e instanceof \PDOException;
self::fail($isDb ? 'db_unavailable' : 'server_error', $isDb ? 503 : 500, [], $ref);
```

→ HTTP **503 `db_unavailable`**，界面上那句「本地数据库暂时不可用，请联系管理员」。

**这句话是错的**。库好得很，是 MySQL 主动挑了一个牺牲者回滚掉 —— 标准处理方式是**重试**，
不是打电话找管理员。`classify()` 那段注释自己写着分类粒度要「按【该去查哪里】来定」，
而这一档恰恰把人指到了完全没有问题的地方。

**② 全仓库没有任何一处重试**

```
$ grep -rn "1213\|40001\|deadlock\|retry" app/lib/ app/api/
（只有几条与本题无关的注释）
```

死锁重试在业务上是**无副作用**的：事务已经整个回滚，重放一次就好。
这套系统一次都没做。

**③ 现有测试结构上抓不到它**

`tests/http_sweep.php::crashed()` 明确把这个响应列为**正确降级**：

```php
// 500 和非 JSON 都算。但 503 + db_unavailable 【不算】——
// 那是库连不上时的正确降级，代码本身没问题。
if ($st === 503 && ($j['error'] ?? '') === 'db_unavailable') { return false; }
```

判断本身没错 —— 错在死锁被塞进了同一个筐里。**探针即使正面撞上死锁，也会打勾放过。**

另外：`php -S` 默认单进程，请求是排队处理的，**在默认配置下永远复现不出并发死锁**。
我是设了 `PHP_CLI_SERVER_WORKERS=8` 才打出来的。现场真机跑的是 PHP-FPM，天然多进程。

### 3.5 数据没坏 —— 这是唯一的好消息

死锁后我核对过账：

```
id 2:  points_balance 5451 == 流水合计 5451   visit_count 2 == 流水计次 2
id 3:  points_balance 5451 == 流水合计 5451   visit_count 2 == 流水计次 2
分了钱但没落流水的半成品单：0 张
```

事务整个回滚了，一分钱没错、一条流水没歪。
所以这是**可用性与信任问题，不是数据问题** —— 但在柜台上，
「记不进去 + 让我找管理员」和「数据坏了」给客人的观感是一样的。

### 3.6 修的方向（供参考，我不改代码）

`grantOne()` 里锁会员之前，按 `member_id` 排一次序 —— 和 `grantMerged()` 里那句
`sort($serials)` 是同一件事、同一个理由。另外两件顺带的：

- `classify()` 补 `1213, 1205 => 'E110'`（或任何一个新码），别再让它冒充「库连不上」；
- 死锁给一次自动重试（最多 1～2 次），业务上完全安全，收银员根本不该看见它。

---

<a id="n5-02"></a>
## 4. N5-02 🔴 撤销记账不回收已发出的十送一券，而且把客人后来真正挣到的那张也一并吞掉

### 4.1 现场是怎么发生的

服务员拿错了卡，把 B 桌的账记到了张三名下。张三因此**正好满了十次**，
系统当场发了一张免费餐券。经理十分钟后发现，到后台**撤销**了那笔记账。

撤销之后：

- 张三的计次退回去了 ✅
- 积分退回去了 ✅
- 累计消费退回去了 ✅
- **那张免费餐券还在张三手上，而且能正常核销** ❌
- **张三后来自己老老实实吃到第十次，一张券都拿不到** ❌

### 4.2 实测（门槛按 3 次跑，逻辑与 10 次完全一致）

```
  记完 RV00                计次 1 · 已发券计数 0 · 券在手 0 张
  记完 RV01                计次 2 · 已发券计数 0 · 券在手 0 张
  记完 RV02                计次 3 · 已发券计数 1 · 券在手 1 张（69C936AF 状态1）

  —— 第 3 次记的是【别人的桌】，经理发现后撤销 ——
  撤销结果：{"ok":true,"reversal_id":1777}
  撤销后                  计次 2 · 已发券计数 1 · 券在手 1 张（69C936AF 状态1）
                                                    ↑ 计次退了，券没退
```

**这张券是真能吃掉的**，不是挂在那儿而已：

```
  客人拿着撤销前发出的那张券 69C936AF 来吃免费餐……
  核销结果：{"ok":true,"code":"69C936AF","member_id":2,"forced":false}
  券的状态：{"status":2,"redeemed_at":"2026-08-31 19:38:05"}
```

**一顿饭就这么送出去了。** 这是这一轮里唯一一条直接对应「店铺亏钱」的问题。

### 4.3 更难受的后半段：客人真正挣到的那张也没了

```
  —— 客人后来自己老老实实吃到第 3 次 ——
  checkAndGrant → 发出 0 张，欠 0 张
  第 3 次（真实的）   计次 3 · 已发券计数 1 · 券在手 1 张

  ▸ 界面上跟客人怎么说？
    「已累计 3 次 / 每 3 次送 1 次，还差 3 次」   该发 1 张 / 已发 1 张 / 欠 0 张
```

客人手里拿着一张「满 3 次送 1 次」的卡，**计次栏明明白白写着 3**，
进度条却告诉他**还差 3 次**。这句话怎么跟客人解释？

### 4.4 经理做了正确的事，也救不回来

我接着试了唯一正确的补救动作 —— 经理到后台把那张发错的券**作废**：

```
  —— 经理发现券发错了，到后台【作废】它 ——
  作废结果：{"ok":true}
  作废之后             计次 2 · rewards_issued 1 · 在手券 0

  —— 客人自己吃到第 3 次 ——
  checkAndGrant → 发 0 张，欠 0 张
  第 3 次（真实的） 计次 3 · rewards_issued 1 · 在手券 0
  进度条上写着：「已累计 3 次 / 每 3 次送 1 次，还差 3 次」

  —— 再吃三次（一共 5 次）——
  累计 5 次             计次 5 · rewards_issued 1 · 在手券 0
  进度条上写着：「已累计 5 次 / 每 3 次送 1 次，还差 1 次」（该发 1 / 已发 1 / 欠 0）
```

**作废券并没有让计数回去。** 客人的第一张免费餐，从「吃 3 次」变成了「吃 6 次」——
按真实门槛就是**从十次变成二十次**。而进度条上那句「还差 1 次」看上去完全正常，
没有任何人会觉得不对。

### 4.5 根因：三处都不回退 `rewards_issued`，而它决定一切

`RewardService::progressOf()`：

```php
$earned  = intdiv($progress, $threshold);   // 按当前规则总共该发几张
$pending = max(0, $earned - $issued);       // 还欠几张
```

`$issued` 就是 `member.rewards_issued`。而：

- `PointsService::reverse()` 回退了 points / visits / amount / 订单已分配额 —— **唯独没碰 `rewards_issued`，也没动券**；
- `RewardService::void()` 把券置为作废 —— **也没碰 `rewards_issued`**；
- `grantManual()` 有意不加 `rewards_issued`（这一条是对的，注释也写清了理由）。

`RewardService.php` 第 253 行的注释其实**已经知道这件事**：

> 券是真金白银的一顿饭，而 `void()` 不回退 `rewards_issued`，发多了只能人工逐张作废。

但那句话是写在「并发会多发券」的语境里的，谈的是**多发**；
这里暴露的是同一个字段在**撤销**语境下的另一半后果 —— **少发**，而且是永久性的。

### 4.6 店里看不见，也修不了

```
$ grep -rn "rewards_issued" app/api/ app/cp/ wwwroot/
（零命中）
```

`rewards_issued` **在任何一个接口、任何一个后台页面上都不出现**。
店里没有任何办法看到「这位客人的已发计数比实际多了 1」，
更没有办法把它改回去 —— 只能直接进数据库改。

唯一的补救是后台手工发一张券（`app/cp/routes.php:432` 的 `grantManual`），
但前提是**有人发现客人被少发了** —— 而进度条那句「还差 1 次」恰恰在掩盖它。

### 4.7 为什么测试没抓住

`tests/cases/RewardTest.php` 与 smoke ⑬/㉛ 覆盖的是「发券的门槛对不对」「冲正后计次对不对」，
**没有一条把「撤销」与「已发券计数」摆在一起看**。
两件事各自都正确，接缝处没人验。

### 4.8 修的方向（供参考）

至少要在三件事里挑一件做出决定，并且写进 `docs/`：

1. `reverse()` 连带作废由这笔记账触发的券（券上已经存了 `source` 与发券时的 `progress`，追得回来），同时 `rewards_issued -= 1`；
2. 或者保留「券发出去就不追回」的宽松政策（对客人友好），但 **`void()` 必须回退 `rewards_issued`**，否则客人后面那张永远补不上；
3. 无论选哪条，`rewards_issued` 都该在后台会员页上**露出来**，并允许经理带原因修正 —— 现在它是一个能让客人白吃亏、而店里完全看不见的隐藏字段。

---

<a id="n5-03"></a>
## 5. N5-03 🟡 新增的「不计次」预览，免费餐那一支说的话在出厂配置下永远兑现不了

### 5.1 新功能本身是对的

`8382d8d` 新增的 `PointsService::visitPreview()`，主干做法我认同，也验过：

- 复用 `visitsFor()` / `countedThisSitting()`，**不另写一份判断** —— 这个决定尤其对，
  提交信息里那句「预览说会计次、结果没计……那比不提示还糟」说得准确；
- `by_portion` / `by_order` 口径下**一律不提示**，因为那两种要等分配填完才知道 —— 这个克制是对的；
- 拿不到订单就返回 `null`，Pad 一律当「会计次」，不吓唬人 —— 兜底方向也对。

### 5.2 问题只在多写的那一支

```php
// 免费餐 / 整单核销：兑换来的那一餐本来就不计次（与 grantOne 同一条判断）
$fullyRedeemed = (int)($order['is_redeemed'] ?? 0) === 1
              && (int)($order['portions_counted'] ?? 0) === 0;
if ((int)($order['is_free_meal'] ?? 0) === 1 || $fullyRedeemed) {
    return ['counts_visit' => false, 'reason' => 'free_meal'];
}
```

注释说「与 `grantOne` 同一条判断」—— **但少看了一个开关**。
`grantOne()` 在同样的情形下是这么走的：

```php
$extraEarns = $this->cfg->get('free_meal_extra_earns', '0') === '1';
...
if ((int)$order['is_free_meal'] === 1 && !$extraEarns) {
    return ['ok' => false, 'error' => 'free_meal'];      // ← 整单拒绝
}
if ($fullyRedeemed && !$extraEarns) {
    return ['ok' => false, 'error' => 'redeemed'];       // ← 整单拒绝
}
```

`free_meal_extra_earns` 的**出厂默认是 `0`**。也就是说：预览说的是「不计次」
（言下之意是**别的照记**），实际是**整单一点都不记**。

### 5.3 实测

```
──────── 订单 FM01（is_free_meal = 1）────────
  ① 选完会员，Pad 拿到的预览：  {"counts_visit": false, "reason": "free_meal"}
  ② 服务员点【仍然记账】之后：  ok= False  error= free_meal  msg= 该订单已标记为免费餐，不积分

──────── 订单 FM02（整单核销）────────
  ① 选完会员，Pad 拿到的预览：  {"counts_visit": false, "reason": "free_meal"}
  ② 服务员点【仍然记账】之后：  ok= False  error= redeemed  msg= 该订单已使用十送一核销，本餐不计次不积分
```

而 Pad 上照着这个预览显示的是（`wwwroot/assets/i18n.js`）：

```
常驻提示： ⚠️ 这一餐是用券兑换的 —— 不计次
确认框：   这一餐是用券兑换的，{cards} 不会计入次数。
           请先告知客人，再决定是否继续记账。
           [ 返回修改 ]  [ 仍然记账 ]
```

**「仍然记账」这个选项在出厂配置下根本不存在** —— 点下去只会撞一堵墙。
服务员照着这句话跟客人说完「这一餐不计次，别的照常」，客人点头，然后提交失败。

### 5.4 目前打不到 —— 但话已经说错了

必须说清楚：**在 Pad 自己的流程里，这一支现在是走不到的。**

`free_meal_extra_earns=0` 时 `PointsService::buildContext()` 会把这类订单标成
`eligible: false`，Pad 在选单页就把它们**置灰不可点**：

```js
b.className = 'card ' + (o.eligible ? 'pickable' : 'disabled');
b.onclick = () => { if (!o.eligible) return toast(reason || T('order.notEligible'), 'err'); ... }
```

所以我把它定为 🟡**潜伏**，不是 🔴。但它仍然要记一笔，理由有三个：

1. 我是**直接调 `/member/search` 打出来的** —— 接口本身照发不误，
   任何一个不经过选单页的调用方（新界面、外部集成、以后加的补录入口）都会踩上；
2. 店家一旦把 `free_meal_extra_earns` 打开**再关回去**，这一支就会在两种配置之间来回真假；
3. 最重要的：这条新功能的立意就是**「不能让预览和实际说两套话」**。
   现在这支恰恰说了两套 —— 而且方向比原来更糟：
   原来是「记完了才告诉你没计次」，这里是**「告诉你只是不计次，其实一分钱都没记」**。

修的方向：`visitPreview()` 里同样读一次 `free_meal_extra_earns`，
关着的时候直接返回 `null`（那种单压根轮不到预览），开着的时候才回 `free_meal`。

---

## 6. 顺带复核过、确认没问题的

这几条是沿着三条线摸过去顺手验的，都**没有**发现问题，一并记下来免得下次重复劳动：

| 检查项 | 结论 |
|---|---|
| 一张券被多台设备同时核销 | ✅ **安全**。6 个并发 `redeem()` → 1 个 `OK 核销成功`，5 个 `coupon_not_active`；券最终 `status=2`，只有一个 `redeemed_at` |
| 同一条流水撤销两次 | ✅ **安全**。第二次返回 `already_reversed`，不会把客人扣穿 |
| `grant()` 在事务里碰 POS（复查2 的 NEW-02） | ✅ **仍然是修好的**。`posNow()` 在 `db->transaction()` **之前**取，注释也钉住了理由 |
| 换卡（挂失 / 损坏 / 过期） | ✅ **积分与计次照常带走**。`replaceCard()` 只换 `card` 行并更新 `member.card_no`，`member_id` 不变，流水与券都挂在 `member_id` 上 |
| 订单在 POS 主库里消失 | ✅ **不自动扣分**，推 `order_vanished` 告警 + `markVerified(3)` 等人工核实（这是有意的设计，`ReconcileService.php:121`） |
| 合并记账的加锁顺序 | ✅ **有 `sort($serials)`**，单会员多订单，不存在 N5-01 那种交叉 |
| `grantManual()` 不计入 `rewards_issued` | ✅ **有意为之且正确**，否则会顶掉客人靠消费挣来的那张 |

---

## 7. 一条测试环境上的提醒

**`php -S` 默认是单进程的。** 它把请求排队一个个处理，
所以任何并发缺陷 —— 死锁、竞态、双发券 —— 在默认的本地开发服务器上
**结构性地复现不出来**，跑多少轮都是绿的。

N5-01 是设了 `PHP_CLI_SERVER_WORKERS=8` 之后才打出来的：

```bash
PHP_CLI_SERVER_WORKERS=8 php -S 127.0.0.1:8910 -t wwwroot
```

现场真机跑 PHP-FPM，天然多进程 —— 也就是说**开发环境比生产环境更宽容**，
而且宽容的正好是这一类最难查的问题。建议把这个环境变量写进
`tests/http_sweep.php` 顶部的用法说明里（那里现在写的是不带 workers 的版本）。

---

## 8. 一条测试夹具上的提醒（不是 bug）

`tests/http_sweep.php` 第 355 行：

```php
$OLD_INVOICE = (int)(getenv('SWEEP_OLD_INVOICE') ?: 92521);   // 真实存在、但已超回溯天数
```

在一个照 `pdb/pos_schema.sql` 新建的夹具库上，92521 这个 `order_head_id` 不存在，
于是 `locateByInvoice()` 走 `not_found` 分支（那一支不返回 `max_days`），
第 391 行那条断言就红了：

```
✗   └ 但回溯天数还给 —— 那是后台配置，经理本来就看得到，用来把话说完整
```

**这不是代码问题** —— 换一个夹具库里真实存在、且超过 `invoice_lookup_max_days` 的
`order_head_id` 就全绿了（我用的是 92400）：

```bash
SWEEP_OLD_INVOICE=92400 php tests/http_sweep.php   → 全站探针通过 82 项
```

但它会让一个干净环境上的第一次运行看起来像是撞了 bug。
建议在那行注释上补一句「换库要跟着换」，或者让探针在 `not_found` 时
打一句「夹具里没有这个单号，本组跳过」。

---

## 9. 附：本轮用到的复现脚本

都在会话临时目录下，**没有进仓库**：

| 脚本 | 用途 |
|---|---|
| `dl_http_setup.php` / `dl_setup4.php` | 造两位会员 + N 对镜像订单 |
| `dl_http_worker.php` | 对齐起跑时刻，走真实 HTTP 并发 `POST /api.php/points/grant`，按 HTTP 状态与 `error` 归类 |
| `rv_setup.php` / `rv_probe.php` | 十送一发券 → 撤销 → 看券还在不在、后面补不补发 |
| `rv_redeem.php` | 把撤销后遗留的那张券真核销掉 |
| `rv_void.php` | 经理作废那张券之后，验证客人后面还是拿不到 |
| `fm_setup.php` / `fm_probe.sh` | 免费餐 / 整单核销订单上，预览说的话 vs 实际提交的结果 |
| `neg_probe.php` | 撤销的幂等性与负余额 |

---

*本报告只新增本文件，未改动仓库中任何既有文件。*
